# 詳細仕様：HPのお問い合わせページ経由のメール

対象カテゴリ：HPのお問い合わせページ経由のメール
親仕様：メールアシスタントツール_仕様書.md（v0.21）2章・4章・5章

作成日: 2026-08-29

## 0. 本カテゴリの位置づけ（他カテゴリとの違い）

他の2カテゴリ（定型的な問い合わせ対応／日程調整・アポイント）は、メール受信をトリガーに**自動で**返信の下書きをGmailに作成する機能だが、本カテゴリは受信時に**自動では**返信の下書きを作成しない。理由は以下の通り。

- HPのお問い合わせフォームからの通知メールは、フォームサービスが自動生成する固定の送信元アドレスから届き、実際の問い合わせ者のメールアドレスとは異なるケースが多い
- 通知メールの差出人にそのまま返信すると、実際の問い合わせ者に届かない・誤った相手に返信してしまうリスクがある

そのため本機能は、受信メールから会社名・担当者名・メールアドレスなどの主要項目を自動で抽出し、ダッシュボードの一覧に整理して表示するところまでを行う。実際の回答内容の作成・送信は利用者が行う。ただし、宛先と結びの文（署名等）だけを自動で入れた下書きを、一覧からワンクリックで作成する補助機能は用意する（Step 6参照）。

## 1. トリガー

GAS（Google Apps Script）の時間主導型トリガーにより、Gmailの新着メールを定期的にポーリングして検知し、処理を開始する（2026-09-15決定。全カテゴリ共通の方式。親仕様4章参照）。ポーリング間隔（スキャン頻度）のデフォルト値は5分とする（2026-09-16決定。下書き自動生成・日程調整アポイント・HP問い合わせ・迷惑メール誤判定検知の4機能共通のデフォルト値。共通基盤_GAS運用の注意点.md参照）。

## 2. 前提・追加の連携要件

Gmail連携のみで完結する。Googleカレンダー等、日程調整・アポイント機能で必要な追加連携（詳細設計_日程調整アポイント.md 2章参照）は本カテゴリでは不要。

## 3. 入力

- メール本文（プレーンテキスト）
- 件名
- 差出人アドレス（フォームサービスの通知用アドレスであることが多く、実際の問い合わせ者のアドレスとは区別して扱う。3章の抽出項目「メールアドレス」参照）
- 宛先アドレス（To。カテゴリ判定の主要項目、Step 2参照）
- 受信日時
- 除外判定用のラベル情報（迷惑メール判定結果）
- **HP問い合わせ宛先アドレス一覧の設定値**（利用者が設定画面で追加・編集できる。例：`info@◯◯.co.jp`、`contact-form@◯◯.co.jp`など、HPのお問い合わせフォームからの通知メールが届く宛先を登録する想定）
- **抽出項目のラベル別名一覧の設定値**（利用者が設定画面で追加・編集できる。例：「お名前」「氏名」「ご担当者名」をいずれも「担当者名」項目として認識させる、など。Step 3参照）

## 4. 処理フロー

### Step 1：除外判定（前処理）

以下に該当するメールは、本機能の処理対象から外す。

- 迷惑メールフォルダに分類されているメール（親仕様6章）

該当する場合はここで処理を打ち切り、迷惑メール誤判定検知機能側の警告・通知フローに委ねる（下書き自動生成・日程調整の詳細設計と共通のルール）。

### Step 2：カテゴリ判定（宛先アドレスベース）

他の2カテゴリが**メール内容**で判定するのに対し、本カテゴリは**宛先アドレス**で判定する点が異なる。

- 受信メールの宛先アドレス（To）が、設定済みの「HP問い合わせ宛先アドレス一覧」に含まれるかどうかを判定する
- **該当する場合、メール内容によらず本カテゴリとして扱う。** 内容が定型的な質問に見えても「定型的な問い合わせ対応」には回さず、日程調整的な要素が含まれていても「日程調整・アポイント」には回さない（決定済み事項。7章参照）
- **判定の優先順位**：全体ワークフロー（親仕様4章）の分類ステップにおいて、本カテゴリの宛先ベース判定は、他カテゴリの内容ベース判定より先に行う
- 該当しない場合は、他カテゴリ（定型的な問い合わせ対応／営業・取引先／日程調整）の判定ロジックに回す。どのカテゴリにも該当しない場合は、通常の未分類の受信メールとして扱う（他カテゴリの詳細設計と共通の切り分け方針）

### Step 3：構造化項目の抽出

フォームからの送信内容は、項目ラベル付きの構造化データとして本文に含まれる想定。以下の項目を抽出する。

- 会社名
- 担当者名（氏名）
- メールアドレス（本文中に記載された、問い合わせ者本人のアドレス。差出人アドレスとは別項目として保持する）
- 電話番号
- お問い合わせ内容（自由記述部分）
- フォーム選択カテゴリ（フォーム側にカテゴリ選択項目がある場合のみ。任意項目。Step 4のAI判定「種別」とは別物）

抽出方法・注意点：

- ラベル表記のゆらぎ（「お名前」「氏名」「ご担当者名」等）に対応するため、3章の「抽出項目のラベル別名一覧」を参照してラベルを正規化してから値を抽出する
- 抽出に失敗した項目（本文中にラベルが見つからない、フォーマットが想定と異なる等）は無理に推測せず、「未取得」として空欄のまま扱う

**実際に確認できている通知メールの例（参考）**

- ラベルと値の区切り文字は「：」ではなく、全角の「＝」（前後に半角スペース）だった（例：「お名前 ＝ 斎藤聖菜」）。抽出ロジックは「：」だけでなく「＝」など、複数の区切りパターンに対応できるようにしておく
- 「会社名」欄は存在せず、「お名前」「フリガナ」「メールアドレス」「電話番号」「メッセージ」の5項目のみだった。フォームによって項目構成が異なることが分かっているため、存在しない項目は無理に埋めず「未取得」として扱う（上記の既存ルール通り）

### Step 4：要約・種別判定（AI生成）

件数が多くなることを想定し、一覧を一目で確認できるよう、Step 3で抽出した「お問い合わせ内容（自由記述部分）」を対象にAIが要約と種別判定を行う。

**要約**

- 自由記述の内容を1〜2文程度に要約する
- 何を目的に連絡してきたのか（質問内容、要望、案内等）が一覧上で一目でわかることを目指す

**種別判定**

以下の3種類のいずれかに分類する。

- **本来の問い合わせ**：自社の製品・サービスについての質問、資料請求、見積依頼など
- **営業・PR目的**：セミナー・カンファレンスの案内、他社サービスの売り込み、広告掲載の提案など、フォームを経由した営業目的の送信（実例：フォーム送信者が「〇〇株式会社の〇〇と申します」と名乗ったうえで、自社主催イベントへの参加を案内してくるケース）
- **その他／判定不能**：上記のいずれにも明確に当てはまらない、または自由記述が短すぎる・欠落している等で判定材料が不足している場合

判定の観点（案）：

- 文面が質問・依頼の形式か、それとも案内・宣伝の形式か
- 名乗りに続けて、自社サービス・イベントの紹介をしていないか
- 「ご案内」「ご紹介」「ご提案」等の営業色の強い語彙が含まれるか
- 自社サービスへの具体的な質問・依頼内容が含まれているか

判定の確信度が低い場合は「その他／判定不能」として扱い、無理に「本来の問い合わせ」「営業・PR目的」のいずれかに寄せない。

### Step 5：ダッシュボードへの一覧反映

- Step 1〜5の自動処理では返信の下書きは作成しない（0章参照。利用者操作によるStep 6は除く）
- 抽出・生成した項目を1件のレコードとして、ダッシュボードの「HP問い合わせ一覧」に追加する
- 一覧の表示項目：受信日時、会社名、担当者名、メールアドレス、電話番号、**要約**（Step 4）、**種別**（本来の問い合わせ／営業・PR目的／その他・判定不能。Step 4）、フォーム選択カテゴリ（Step 3）、元メールへのリンク、対応状況
  - 従来案にあった「お問い合わせ内容（冒頭の一部）」表示は要約に置き換える。全文の確認は元メールへのリンクから行う
  - **種別（本来の問い合わせ／営業・PR目的／その他・判定不能）で一覧を絞り込めるフィルタ機能を用意する（決定済み事項）。タブ分けは行わず、同じ一覧内でのフィルタ表示とする**
- **対応状況は「未対応／対応済み／保留」の3種類のバッジとし、ダッシュボード上で利用者がクリックして切り替えられるようにする**（決定済み事項。他カテゴリの「下書き作成済み・未送信」ステータスとは別の管理軸。具体的なUI仕様はダッシュボード機能の詳細設計時に定める。8章「決定済み事項」参照）

### Step 6：返信下書きの補助作成（利用者操作・任意）

Step 1〜5は新着メール検知をトリガーに自動で実行されるが、本Stepは利用者がダッシュボードの「HP問い合わせ一覧」上で操作したときにのみ実行される（決定済み事項）。

- 一覧の各行に「返信下書きを作成」ボタンを設置する（専用の一覧は別途作らず、既存のHP問い合わせ一覧内に置く）
- ボタンを押すと、Step 3で抽出した問い合わせ者本人のメールアドレス宛に、Gmailの**新規メール**として下書きを作成する（元の通知メールへの返信ではなく新規メールとする。理由：元メールはフォームサービスからの通知メールであり、問い合わせ者本人とのスレッドではないため）
- 下書きに自動で入れる内容は、**宛先（To）と、末尾の結びの文・署名のみ**とする。**質問への回答本文（中身）は自動生成しない**。空欄のまま利用者が手動で記入する運用とする
- 下書き作成後、一覧上に「下書き作成済み」であることが分かる表示を追加する（対応状況バッジとは別の表示とする想定。具体的なUI仕様はダッシュボード機能の詳細設計時に定める）

## 5. 出力

- ダッシュボードの「HP問い合わせ一覧」への新規レコード追加（抽出項目一式＋AI生成の要約・種別、Step 3・4参照）
- 新着メール検知時にGmail下書きは作成しない（本カテゴリ固有の仕様。0章参照）
- （Step 6実行時のみ）Gmail下書き（新規メール、宛先＝抽出したメールアドレス、本文は結び・署名のみ）
- 集計用のメタ情報（カテゴリ、種別判定の結果、抽出が全項目成功したか一部失敗したかなど。親仕様5章の全体サマリーグラフ「ホームページからの問い合わせメール件数」の集計対象）

## 6. 例外処理

- **宛先はHP問い合わせ宛先一覧に該当するが、想定フォーマットと異なり主要項目（お問い合わせ内容など）がほとんど抽出できない場合**：抽出できた範囲のみで一覧に追加し、「フォーマット未認識」等のフラグを立てて表示する（利用者が元メールを直接確認しやすくするため）
- **要約・種別判定に失敗した場合**（自由記述が空、AI処理のエラーなど）：要約欄は空欄、種別は「その他／判定不能」として記録する。この部分の失敗だけを理由に、他の抽出項目・一覧への反映自体は保留にしない
- **同一の問い合わせに対して重複通知が来た場合**（フォームの二重送信など）：**重複排除は行わず、そのまま複数レコードとして一覧に追加する（決定済み事項。重複送信自体の件数が多くなることは想定されないための判断）**
- **抽出処理自体がエラーになった場合**（システムエラー等でStep 3以降の処理が完了しなかった場合）：エラーとして記録したうえで、機械的に取得できる範囲の情報（受信日時、差出人、件名、元メールへのリンク）のみでレコードを作成し、ダッシュボードの「HP問い合わせ一覧」に「処理エラー」フラグを立てて表示する。対応状況は「未対応」とする。あわせて処理エラーアラート一覧（詳細設計_ダッシュボード.md 9章）にも記録し、カテゴリを横断してエラー発生状況を確認できるようにする（2026-09決定）
  - 本カテゴリは専用の一覧（HP問い合わせ一覧）を持つため、他カテゴリ（下書き自動生成・日程調整）のように通常の未対応メールとして受信トレイ側にのみ残すのではなく、一覧上でエラーに気づけるようにする（他カテゴリの詳細設計とは異なる、本カテゴリ固有の扱い）
- **Gmail連携の認可（OAuth）が失効している場合**：GASでは「Authorization is required to perform that action」等の特定メッセージを含む例外が発生するため、try/catchで検知し、上記の一般的な処理エラーとは区別して処理エラーアラート一覧に「要再認証」種別で記録する（2026-09-15決定）。この種のエラーは放置しても自然には復旧せず、人手による再認可操作が必要となるため区別する。全カテゴリ共通の方針（詳細は`共通基盤_GAS運用の注意点.md`、`メールアシスタントツール_仕様書.md` 7章「Gmail連携の認可エラー対応方針」参照）

## 7. 他機能との関係

- ダッシュボード（親仕様5章）：本カテゴリ専用の「HP問い合わせ一覧」表示、全体サマリーグラフの「ホームページからの問い合わせメール件数」の集計元
- 下書き自動生成（定型的な問い合わせ対応）：宛先アドレスがHP問い合わせ宛先一覧に該当する場合、内容が定型的な質問に見えても本カテゴリとして扱い、下書き自動生成のロジックには回さない（決定済み、Step 2参照）
- 日程調整・アポイント：同様に、HP問い合わせ宛先からのメールは内容に日程調整的な要素が含まれていても本カテゴリとして扱う（宛先ベースの判定が内容ベースの判定より優先される、Step 2参照）
- モードB（全自動送信、親仕様3章）：本カテゴリは新着メール検知時に自動では下書きを作らない設計のため、当面モードBの対象外。Step 6の下書き作成も利用者のボタン操作が起点であり、全自動化ではない。将来、抽出精度が上がれば「抽出結果の自動通知」的な形での自動化余地はあるが、フェーズ1では対象外とする

## 8. このカテゴリ固有のOpen Questions

- 手動訂正履歴をAIの判定プロンプトの参考例（few-shot例）として反映する案（下記「決定済み事項」参照）を採用するかどうか、フェーズ1に含めるか
- 月次サマリー表示（下記「決定済み事項」参照）の実装要否・実装時期。フェーズ1を実際に運用し、手動訂正がどの程度発生するか見たうえで判断する

### 決定済み事項（参考）

以下は検討の結果、本ドキュメント内で決定済みとした項目（旧Open Questions）。ダッシュボード機能自体の詳細設計時に、UI仕様として反映すること。

- **抽出ルール（区切り文字パターン・項目構成）の追加方針** → **今回確認できた1パターン（Step 3参照）をもとに設計しておき、今後複数のフォームサービス・複数のHPを扱うことになって未知のパターンが出てきたら、その都度ルールを追加していく運用とする**（先取りした汎用化までは行わない）
- **HP問い合わせ宛先アドレス一覧・抽出項目のラベル別名一覧の設定画面** → **日程調整のキーワード一覧設定（詳細設計_日程調整アポイント.md 4章Step 2）と同様の仕組みを流用する**。件数がそれほど多く発生するとは見込んでいないため、専用の凝った設定UIは作り込まない
- **「HP問い合わせ一覧」の対応状況の管理粒度** → **「未対応／対応済み／保留」の3種類のバッジとし、ダッシュボード上でクリックして切り替えられるようにする**（Step 5）。他カテゴリで想定している「下書き作成済み・未送信」等のステータスとは別の管理軸として扱う
- **重複送信（フォームの二重送信等）の検知・排除** → **行わない。重複排除のロジックは設けず、そのまま複数レコードとして一覧に追加する**（重複送信自体の件数が多くなることは想定されないための判断。6章参照）
- **一覧からのワンクリック返信下書き作成** → **作る**（Step 6）。宛先（To）と結びの文・署名のみを自動生成し、質問への回答本文は自動生成しない（利用者が手動で記入）。作成は元メールへの返信ではなく、抽出したメールアドレス宛の新規メールとする
- **種別判定の誤判定への対応** → **ダッシュボード上で利用者が種別を手動訂正できるようにする（フェーズ1で実装）**
- **精度向上策（手動訂正より先の施策）の採否**：
  - 訂正が多かったパターン（差出人ドメイン、頻出フレーズなど）を判定観点のキーワードリストに追記できるようにする（Step 4の判定観点は現状固定のため、これをHP問い合わせ宛先一覧などと同様に利用者が編集できる設定リスト化する） → **今後のフェーズで検討（フェーズ1では実装しない）**
  - AIに判定の確信度も出力させ、確信度が低いものは一覧上で「要確認」として目立たせる → **見送り（不採用）**。本カテゴリは種別判定の結果をもとに自動送信・自動処理を行うわけではなく、誤判定があっても表示ラベルが違う程度の影響にとどまるため、確信度の可視化までは作り込まない（ただしStep 5のフィルタ機能に頼って一覧を絞り込む場合、誤って「営業・PR目的」に分類された本来の問い合わせを見落とすリスクはゼロではない点は留意する）
  - 利用者による手動訂正の履歴を蓄積し、判定プロンプトの参考例（few-shot例）として定期的に反映する → **未定**（8章Open Questions参照）
  - 月次など定期的に、手動訂正が発生した件数・傾向をダッシュボードのサマリーに表示し、判定ロジックの見直し時期の目安にする → **フェーズ1を実際に運用し、手動訂正の発生状況を見たうえで実装要否・時期を判断する**（8章Open Questions参照。時期は未定）
- **「営業・PR目的」と判定した件の一覧上での扱い** → **一覧に種別で絞り込めるフィルタ機能を用意する。タブ分けは行わない**（Step 5）
