# 詳細仕様：下書き自動生成機能（コア機能）

対象カテゴリ：定型的な問い合わせ対応
親仕様：メールアシスタントツール_仕様書.md（v0.21）2章・4章

作成日: 2026-08-16

## 1. トリガー

GAS（Google Apps Script）の時間主導型トリガーにより、Gmailの新着メールを定期的にポーリングして検知し、処理を開始する（2026-09-15決定。全カテゴリ共通の方式。親仕様4章参照）。ポーリング間隔（スキャン頻度）のデフォルト値は5分とする（2026-09-16決定。下書き自動生成・日程調整アポイント・特定受信アドレス・迷惑メール誤判定検知の4機能共通のデフォルト値。共通基盤_GAS運用の注意点.md参照）。

## 2. 入力

- メール本文（プレーンテキスト）
- 件名
- 差出人アドレス
- 受信日時
- スレッド履歴（同一スレッド内の過去のやり取り。あれば）
- 除外判定用のラベル情報（迷惑メール判定結果）

## 3. 処理フロー

### Step 1：除外判定（前処理）
以下に該当するメールは、本機能の処理対象から外す。

- 迷惑メールフォルダに分類されているメール（親仕様6章）

該当する場合、そのメールについてはここで処理を打ち切り（他の受信メールの処理には影響しない）、迷惑メール誤判定検知機能側の警告・通知フローに委ねる。下書き自動生成は行わない。

### Step 2：カテゴリ判定

本カテゴリ「定型的な問い合わせ対応」への該当判定は、**AIによる判定のみ**で行う（ルールベースの事前フィルタは設けない。2026-09決定）。

**処理順序**：親仕様4章のカテゴリ判定は、①特定受信アドレス宛のメール（宛先アドレス一致、ルールベース。詳細設計_特定受信アドレス.md参照）→②日程調整・アポイント（キーワード一致、ルールベース。詳細設計_日程調整アポイント.md参照）→③本カテゴリ（AI判定）の順に行う（親仕様2章「カテゴリ判定の処理順序・判定方式」参照）。本Step2は、①特定受信アドレス宛のメール・②日程調整・アポイントのいずれにも該当しなかったメールに対してのみ実行される。

判定の観点（AIへの判断材料）：
- 質問文・依頼文の形式になっているか（例：「〜について教えてください」「〜を送っていただけますか」）
- 差出人が初めての相手か、既知の取引先か（初めての相手からの単発的な質問は本カテゴリに該当しやすい）。**この判定のためにGoogleの連絡先（Google Contacts）等の外部データと機械的に照合する仕組みは設けない。あくまでAIへの判断材料の1つとして扱うのみとする**（2026-09決定。その後どのみちAI判定が入るため、別途の連絡先連携は不要と判断）
- 過去に類似パターンの問い合わせ・回答実績があるか

上記の観点をもとに、実際にAIへ渡すプロンプトは「当てはまりやすい特徴／当てはまりにくい特徴」を文章で示し、判定対象のメール（件名・本文・差出人・スレッド履歴の有無）を続けて記載し、「該当する／該当しない」＋理由を返してもらう形とする（2026-09決定。プロンプトの型自体は決定済み）。

「当てはまりやすい特徴／当てはまりにくい特徴」の具体的な中身（リストの実際の文言）は、日程調整・アポイントのキーワード一覧や特定受信アドレス一覧と同様、コードに固定で埋め込まず、利用者が設定画面から追加・編集できるリストとして持たせる（2026-09決定）。実行時はプログラムがこの一覧を読み込み、AIへのプロンプト文中に差し込む。

リストの中身の作り方（方法論）は、理屈で先に決めるのではなく、実際の受信メールから逆算する（2026-09決定）：①うねのさんの受信トレイから問い合わせらしきメールを10〜20通程度ピックアップする→②「該当（定型的な問い合わせ）」「非該当（進行中の案件・クレーム等）」に手作業で仕分ける→③それぞれのグループに共通する特徴（初めての相手か、質問が短く単発か、進行中のやり取りの続きか等）を言葉にして、上記のリストに登録する。この作業自体はまだ未着手で、実際のメールを使ってリストの中身を作るところが7章Open Questionsの残課題として残っている。

**判定結果の確定方法（確信度としきい値）**：AIには、「該当する／該当しない」の一次判定に加えて、その判定に対する確信度（0〜1の数値）もあわせて返してもらう。この確信度を、設定画面で設定したしきい値と比較し、しきい値以上であれば「該当する」として本カテゴリの処理を続行する。しきい値未満の場合は、AIの一次判定が「該当しない」であっても「確信が持てない」であっても区別せず、まとめて「未分類メール」として扱う（2026-09決定。「該当しない」と「確信が持てない」はいずれにせよ同じ「未分類メール」という結果になるため、しきい値は1本で足りる）。しきい値の初期値は0.5とする（2026-09決定）。確信度の数値は、メールごとの集計用メタ情報として保存する（4章参照）。

なお、上記の「処理順序」で述べた①特定受信アドレス宛のメール・②日程調整・アポイントは本Step2より前に判定済みのため、本Step2で「未分類メール」となったメールは、カテゴリ判定全体（親仕様4章）における実質的な最終結果となる（親仕様2章「分類結果の扱い」参照）。

**ダッシュボードからの一時的なしきい値変更による再処理**：ダッシュボードでは、上記の恒久的なしきい値（設定画面の値）そのものを変更するのではなく、再処理を実行するその場限りの一時的なしきい値を指定できるようにする（2026-09決定。「設定を変えて本運用に反映する」操作と、「試しに条件を変えて見直す」操作は分けて扱う）。対象範囲は、受信日時の期間（いつからいつまで）を指定する形とする（2026-09決定）。指定した期間内のメールについて、保存済みの確信度と、その場で指定した一時しきい値を再比較するだけで再判定できるため、AIへの再判定リクエスト（追加のAPI呼び出し）は発生しない。

- **未分類→該当するに格上げされる場合**（一時しきい値を下げて再処理した場合）：対象メールについてStep3（回答内容の特定）以降の処理を新たに実行し、下書きを作成する
- **該当する→未分類に格下げされる場合**（一時しきい値を上げて再処理した場合）：対象メールについて、既にGmailに作成済みの下書きがあれば自動的に削除し、ダッシュボードのステータスを「未分類メール」に更新する（2026-09決定。しきい値の見直しによる誤判定の是正とみなし、下書きも合わせて削除する）

この一時的な再処理の結果を見て、恒久的なしきい値の設定値自体を変更するかどうかは、利用者が別途判断し、設定画面から変更する（再処理の操作そのものとは切り離された、別の判断・別の操作とする）。

この判定結果（該当／未分類）は、ダッシュボードのカテゴリ別受信件数集計に反映するとともに、判定方式（本カテゴリは「AI判定」）をバッジ表示する（親仕様5章「判定方式のバッジ表示」参照）。

### Step 3：回答内容の特定
質問内容を解析し、何についての問い合わせか（回答すべきテーマ）を特定する。

**フェーズ1では、AIが実際の回答内容そのものを自動生成する仕組みは持たない（2026-09決定。そもそも論として以下の理由による）**：

- 本ツールには、料金・対応可否・納期といった回答のもとになる、信頼できるデータ基盤（FAQ・ナレッジベース・確定済みの回答テンプレート等）が現状存在せず、フェーズ1でも整備しない前提のため、AIに回答内容を生成させてもその正しさを裏付ける根拠がない
- Gmail送信履歴を検索して過去の類似回答を流用する案も検討したが、（a）今回の質問と本当に同じ内容かどうかの一致判定の精度に不安が残ること、（b）料金・納期のように状況が変わり得る内容は、過去の回答をそのまま使うとかえって古い・誤った情報になるリスクがあることから、採用しない（2026-09決定）
- 本ツールは自動送信を行わず、必ず利用者が下書きを確認してから送信する運用だが、「それらしく書かれた回答文」が下書きに入っていると、実際には根拠のない内容であっても確認時に正しい回答であるかのように見えてしまい、見落として誤ってそのまま送信してしまうリスクがある。信頼できるデータ基盤がない状態でAIに回答内容を作文させること自体が、現状の仕組みには不適切と判断する

以上により、フェーズ1では下書き本文の回答部分を「〇〇の件につきましては」＋`<<<<〇〇について回答不確定>>>>`という定型の形に統一し、Step3ではAIが質問の内容（〇〇の部分）を特定してマーカーに反映するところまでを行う（2026-09決定。マーカー自体の表記・色付けの仕様は下記および4章Step4参照）。

- 質問が1つの単純な問い合わせの場合：「〇〇の件につきましては」＋`<<<<〇〇について回答不確定>>>>`のみで本文の回答部分を構成する
- 質問が複数・複合的な内容の場合：宛名・結び・署名は通常通り作成した上で、質問ごとに同様の形（テーマ＋マーカー）を並べる（5章「例外処理」参照）
- マーカー部分は、下書き本文中で**赤文字＋黄色背景**のハイライト表示にする（2026-09決定）。プレーンな`<<<< >>>>`表記だけでは見落とすおそれがあるため、色付けにより視覚的にも一段と目立たせ、確認漏れを防ぐ

このマーカーを含む下書きは、ダッシュボードの「要確認の下書き一覧」で検出・フラグ表示し、利用者が優先的に確認すべき下書きをひと目で分かるようにする（詳細設計_ダッシュボード.md 3章参照。2026-09決定）。

実際の回答内容をAIに生成させる機能自体（Gmail送信履歴検索・FAQ／ナレッジベースの活用を含む）は、そうした信頼できるデータ基盤が将来整備された段階（フェーズ2以降）で改めて検討する。2026-09時点では、うねのさん自身の所感として、この機能（回答内容の自動生成）はそもそも不要ではないかという方向に傾いており、7章Open Questionsに検討事項として残す。

### Step 4：下書き文面の生成
特定した回答内容をもとに、返信文面を作成する。

- 文体は、利用者の過去の送信メールの文体を参考にする（親仕様7章 Open Questions「文脈・文体の参照」と連動。フェーズ1でどこまで実装するかは別途検討）
- 構成：宛名、本文（質問への回答）、結びの文、署名
- 3章Step3の不確定マーカーを赤文字・黄色背景でハイライト表示するため、下書き本文はHTML形式で生成する（マーカー部分にのみ文字色・背景色のインラインスタイルを適用し、それ以外の本文は通常表示とする。2026-09決定）
- 質問が複数・複合的な内容の場合の下書き構成については、5章「例外処理」参照

### Step 5：下書き保存・通知
- Gmailに、元メールへの返信として下書きを保存する
- 添付ファイル忘れ検知機能との連携は、フェーズ1では見送るため実装しない（親仕様2章「今回は見送る機能」参照。機能が実装された際に本ステップと連携予定）
- ダッシュボードのステータスを「下書き作成済み・未送信」に更新する（親仕様5章「要確認の下書き」の告知）

## 4. 出力

- Gmail下書き（元メールへの返信形式。HTML形式で生成し、不確定マーカー部分は赤文字・黄色背景でハイライト表示。3章Step3・Step4参照）
- ダッシュボードのステータス更新（対象メールを「下書き作成済み」に）
- 集計用のメタ情報（カテゴリ、判定の確信度、下書きに不確定マーカーを含むかなど。親仕様5章の全体サマリーグラフの集計対象。判定の確信度はStep2のしきい値再判定にも利用する）
- カテゴリ判定結果（該当／未分類）の集計値。親仕様5章「カテゴリ別受信件数」グラフの集計対象

## 5. 例外処理

- **質問の内容にかかわらず、回答内容そのもの（質問テーマの具体的な中身）はAIが自動生成しない（2026-09決定。3章Step3参照）**：質問が複数・複合的な内容の場合を含め、判明した質問テーマ（〇〇の部分）ごとに`<<<<〇〇について回答不確定>>>>`という定型マーカー（何が不確定かのヒント付き、赤文字・黄色背景でハイライト表示）を入れた下書きを必ず作成する（保留にはしない。3章Step3・Step4参照）
- **同一スレッド内に既に未送信の下書きが存在する場合**：重複して新しい下書きを作らない
- **下書き生成処理自体がエラーになった場合**：エラーとして記録し、通常の未対応メールとして残す（ダッシュボード上は「未対応」のまま）。あわせて処理エラーアラート一覧（詳細設計_ダッシュボード.md 9章）にも記録し、利用者がカテゴリを横断してエラー発生状況を確認できるようにする（2026-09決定）
- **Gmail連携の認可（OAuth）が失効している場合**：GASでは「Authorization is required to perform that action」等の特定メッセージを含む例外が発生するため、try/catchで検知し、通常の処理エラーとは区別して処理エラーアラート一覧に「要再認証」種別で記録する（2026-09-15決定）。この種のエラーは放置しても自然には復旧せず、人手による再認可操作が必要となるため区別する。全カテゴリ（下書き自動生成・日程調整アポイント・特定受信アドレス・迷惑メール誤判定検知）共通の方針（詳細は`共通基盤_GAS運用の注意点.md`、`メールアシスタントツール_仕様書.md` 7章「Gmail連携の認可エラー対応方針」参照）

## 6. 他機能との関係

- 添付ファイル忘れ検知機能：フェーズ1では見送りのため連携なし（親仕様2章参照。将来実装時に下書き保存前のチェックとして連携予定）
- ダッシュボード（親仕様5章）：「要確認の下書き」の告知（不確定マーカーを含む下書きのフラグ表示を含む。3章参照）、全体サマリーの「自動返信下書き機能が動いた件数」の集計元。システム設定画面（詳細設計_ダッシュボード.md 11章）からStep2の恒久的なしきい値を変更でき、別途ダッシュボードの一時的な再処理機能（画面配置は13章Open Questions参照）からStep2の再判定を行う
- モードB（全自動送信、親仕様3章）：将来このロジックの判定確信度が、モードBへ昇格させるかどうかの判断材料の一つになる想定

## 7. このカテゴリ固有のOpen Questions

- 回答内容そのものをAIに自動生成させる機能（Gmail送信履歴の検索やFAQ・ナレッジベースを情報源とする案を含む）を将来追加するかどうか。フェーズ1では信頼できるデータ基盤がなく、「それらしく見えるが根拠のない回答文」による誤送信リスクの方が大きいと判断し、この仕組み自体を持たないことに決定した（2026-09決定。3章Step3参照）。フェーズ2以降、FAQ・ナレッジベース等のデータ基盤を整備した場合に改めて検討する。2026-09時点では、うねのさん自身の所感として、この機能（回答内容の自動生成）はそもそも不要ではないかという方向に傾いている
- 「定型的」と判定する基準リストの実際の中身（文言そのもの）：プロンプトの型・リストを設定画面で編集可能にする方針・作り方の方法論は3章Step2で決定済みであり、設計としては完了している。残っているのは、以下の方法論に沿って実際にリストの中身を作る作業のみ（2026-09時点で未着手）
  1. うねのさんの受信トレイから問い合わせっぽいメールを10〜20通ピックアップする
  2. それを「該当（定型的な問い合わせ）」「非該当（進行中の案件・クレーム等）」に手作業で仕分ける
  3. それぞれのグループに共通する特徴（初めての相手か、質問が1〜2文で短いか、進行中のやり取りの続きか、等）を言葉にする
  4. 言葉にした特徴を、設定画面の「該当しやすい特徴／該当しにくい特徴」リストに登録する
- しきい値の一時的な再処理機能をダッシュボードのどの画面に置くか（現状「未分類メール一覧」に相当する専用画面がないため、新設要否も含めて検討。詳細設計_ダッシュボード.md 13章参照）
