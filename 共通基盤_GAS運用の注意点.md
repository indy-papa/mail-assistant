# 共通基盤：GAS運用の注意点

作成日: 2026-09-15
関連：検証_迷惑メール誤判定検知_実装方式.md（この内容のもとになった調査）／メールアシスタントツール_仕様書.md 7章「Gmail連携の認可エラー対応方針」

## 0. 本ドキュメントの位置づけ

本ドキュメントは、Google Apps Script（GAS）を使ってGmail等と連携する機能に共通する運用上の注意点（クオータ・安定性・認可エラー対応・監視の仕組み）をまとめたもの。特定の機能（カテゴリ）専用の内容ではなく、GASを採用する機能であれば共通して当てはまる内容を集約している。

**新着メール検知の連携方式は、下書き自動生成・日程調整アポイント・特定受信アドレス・迷惑メール誤判定検知の全カテゴリでGASに統一する方針が決定している**（2026-09-15決定。`メールアシスタントツール_仕様書.md` 4章・7章参照）。迷惑メール誤判定検知機能はGmailコネクタでスパムフォルダにアクセスできないためGAS採用が必須だったが、他の3機能についても、GAS共通の運用面での対応（監視・認可エラー対応の共通化）や実装・保守の効率、うねのさん自身のGAS開発実績（Ysystems向け求人管理システム）との親和性を踏まえ、同じくGASで統一することとした。本ドキュメントの内容は、全4機能に共通して適用される。

## 1. クオータ・制限（consumer個人アカウント基準）

| 項目 | 個人アカウント(consumer) | Workspaceアカウント |
|---|---|---|
| スクリプト1回あたりの実行時間上限 | 6分 | 6分（同じ） |
| トリガーの1日あたり合計実行時間 | 90分 | 6時間 |
| 1スクリプトあたりのトリガー最大数 | 20個 | 20個（同じ） |
| UrlFetchApp（外部API呼び出し）の1日あたり回数 | 20,000回 | 100,000回 |
| MailApp/GmailAppでのメール送信数 | 100通/日 | 1,500通/日 |

（出典：[ModelMonkey - Google Apps Script Quotas & Limits 2026](https://modelmonkey.io/blog/google-apps-script-quotas-limits-2026)。GmailAppでの「読み取り」自体の回数上限は公式ドキュメント上で明確な数値が見当たらなかったが、実務上は送信数クオータほど厳しく制限されていないとする情報が一般的）

うねのさんのGmailアカウントは個人アカウント想定のため、consumer（個人向け）のクオータが基準となる。

**一般的な考え方**：本ツールのように「前回チェック時刻以降の新着分のみを処理する差分方式」であれば、個人利用規模（1日あたり数件〜数十件程度の処理）では、実行時間（6分/回・90分/日）にも外部API呼び出し上限（20,000回/日）にも通常は届かない。ただし、**導入初回に既存の大量データを一括処理する場合は6分の壁に接近しうる**ため、機能ごとに「初回のみバッチを分割する」等の対応が必要かどうかを個別に検討すること（迷惑メール誤判定検知機能での具体的な試算例は`検証_迷惑メール誤判定検知_実装方式.md` 3.2参照）。

**時間主導型トリガーのスキャン間隔（ポーリング頻度）のデフォルト値は、4機能（下書き自動生成・日程調整アポイント・特定受信アドレス・迷惑メール誤判定検知）共通で5分とする（2026-09-16決定）。** 5分間隔で1日実行すると最大288回のトリガー起動になるが、差分方式で新着がない場合の1回あたりの実行時間は数秒程度と短いため、1日あたりの合計実行時間（90分/日の枠）には通常は十分収まる想定。

## 2. 安定性・運用上の注意点

GASのトリガー不安定性について調査したところ、性質の異なる2つの問題が存在することがわかった。いずれも「発生頻度」を定量的に示す公式データは見当たらなかったため、その点は正直に「不明」として扱う。

### (a) 特定の実装パターンで再現する不具合（回避可能）

「トリガーによって起動された関数の中で、さらに新しいトリガーを作成する（`ScriptApp.newTrigger()`を呼ぶ）」という書き方をした場合に、Chrome V8ランタイム下でトリガーが無効化される事例が報告されている（[Qiita](https://qiita.com/yun_bow/items/c9d9529cf3809a25196f)）。この報告では「1回目の自動実行は動くが、2回目が発生しない」という形で**ほぼ毎回再現する**とされている。ただし裏を返せば、トリガーの設定をTriggersメニュー（またはコード）で最初に1回だけ行い、実行のたびにトリガーを作り直さない設計にすれば、このパターン自体を踏まずに済む。「時間主導型トリガーを1つ設定して定期実行する」だけの単純な構成であれば、このパターンには該当しない見込み。なお元記事は2022年時点の情報で、2026年現在も同様の挙動かは未確認。

### (b) 原因不明・頻度不定で認可エラーにより停止する不具合（回避不可、復旧は手動）

これとは別に、Google公式コミュニティフォーラムでは「トリガーが時々"Authorization is required to perform that action"（認可が必要）というエラーで停止する」という報告が複数寄せられている（[Google Apps Script Community](https://groups.google.com/g/google-apps-script-community/c/7EhJASEnJHY)）。報告者ごとの実行間隔はさまざま（10〜15分間隔、1時間〜1日間隔、週1回など）で、頻度に一貫した規則性はなく「ランダムに発生している」と報告者自身が述べている。Google公式からの原因説明・修正報告は確認できなかった。

この(b)の不具合は「大きな問題かどうか」「自然に復旧するか」という観点で見ると、看過しにくい性質を持つ。報告によれば、停止したトリガーは放置しても自然には復旧せず、**スクリプトエディタでトリガーを開いて再編集・再認可する（＝人手による操作）ことで初めて復旧する**、とされている。つまり「一時的に詰まってすぐ自己回復する」類の軽微な不具合ではなく、**気づかないまま放置すると、機能が止まったままになりうる**タイプの不具合である。発生頻度自体は不明（多くのユーザーで起きるのか、ごく一部でしか起きないのかも未確認）だが、「起きたときの影響（自然回復しない・手動対応が必要）」は軽視できない。

以上を踏まえると、**「発生確率は低いかもしれないが、起きたときに気づく仕組みは必須」**という位置づけになる。GASを採用する機能では、5章で述べる「最終実行日時の監視」の仕組みを、努力目標ではなく必須要件として組み込むことを推奨する。

- **本番運用の実績情報が薄い**：調査した範囲の記事はいずれも個人ブログ・note記事・コミュニティフォーラムレベルの情報であり、「本番運用でどの程度の頻度・影響度で発生するか」の定量的な実績データは見つからなかった。うねのさん自身がYsystems向けにGAS採用の求人管理システムを既に運用している実績（/areas/jobs-management-system.md）があるため、その経験値（実際にこの種の停止に遭遇したことがあるか）を踏まえて判断するのが実務的には最も確実。

### (c) 参考：この種の問題はGASに限らず、Gmail連携全般に共通する業界共通の課題でもある

念のため、Zapier・n8n等の他のGmail連携ツールでも同種の「接続が切れて再認証が必要になる」問題が起きていないか確認したところ、これらのコミュニティフォーラムでも「Gmail連携が突然切れて"reconnect your account"と表示される」という報告が多数見られた（[Zapier Community](https://community.zapier.com/authentication-119)、[n8n Community](https://community.n8n.io/t/gmail-trigger-built-in-google-oauth-token-expired-revoked/245114)）。つまり「GASだから特に壊れやすい」というより、**Google側のOAuthトークンの仕様上、Gmail連携全般が本質的に抱えている課題**という側面が大きいとわかった。

Googleの公式なOAuthリフレッシュトークンの失効条件（[Unipile](https://www.unipile.com/google-oauth-refresh-token/)）は以下の通り：
- 未検証（テスト状態）のOAuthアプリは一律7日で失効（ただし「自分のGoogleアカウントで自分用のGASスクリプトを動かす」形態は、外部公開用のOAuthクライアント検証プロセスとは扱いが異なり、この7日ルールには通常該当しない）
- 検証済み・本番状態のアプリでも、以下の条件でトークンが失効する：6ヶ月間使われないと失効／利用者本人がGoogleアカウント設定からアクセスを取り消した場合／Gmail等の機密性の高いスコープでは、Googleアカウントのパスワードを変更した場合／同一クライアントに対して発行されたトークンが51個目以降になった場合、古いものから失効／Google Workspace管理者が組織単位で一括取り消しした場合

結論として、**「Gmail連携が時々切れて再認可が必要になる」こと自体は、GAS固有の欠陥ではなく業界共通の前提条件**である。yaritori・楽楽メール等の商用メール処理ツール（`メールアシスタントツール_仕様書.md` 8章で言及した競合）がこの問題を「抱えていない」わけではなく、彼らはエンジニアリングチームが監視・アラート・ワンクリック再接続のUXを作り込むことで、利用者からは問題が見えないようにしているだけ、と考えるのが実態に近い。つまり本ドキュメントで提案している「最終実行日時の監視」は、場当たり的な対策ではなく、この業界共通の課題に対する標準的な対処法そのものである。

### (d) 再認可の具体的な操作と難易度

OAuth認可は「本人が実際にログインして許可ボタンを押す」仕組みそのものであるため、失効後はスクリプト側だけでは絶対に自動復旧できない（設計上の意図的な制約）。GASの場合の具体的な手順は次の通り（[公式トラブルシューティングガイド](https://developers.google.com/apps-script/guides/support/troubleshooting)）：

1. script.google.comで対象プロジェクトを開く
2. エディタ上で適当な関数を1つ選んで「実行」する（または「トリガー」画面から該当トリガーを開き直す）
3. Googleの認可ダイアログが表示されるので、アカウントを選択
4. 個人用スクリプト（Google未検証アプリ）のため「このアプリはGoogleで確認されていません」という警告画面が出る。「詳細」→「（プロジェクト名）に移動（安全ではないページ）」の順にクリックする必要がある
5. 権限内容を確認し「許可」をクリックして完了

所要時間は1〜2分程度で、操作自体は難しくない。**うねのさんのようにIT経験がある利用者にとっては全く手間取らない作業**。ただし、手順4の「このアプリは確認されていません／安全ではないページに移動」という警告文言は、セキュリティに詳しくない一般利用者には「危険な操作をしているのでは」と不安にさせやすく、つまずきやすいポイントである。フェーズ1（個人利用）では問題にならないが、**将来の企業向け展開（`メールアシスタントツール_仕様書.md` 8章、BYOK・非エンジニアの利用者を想定）では、この再認可操作を利用者自身に行わせるのは現実的でない**可能性が高い。その段階では、Zapier等のように製品側に「再接続」ボタンを用意し、内部でOAuthフローを完結させるUXへの作り替えが必要になる（Gmail API直接連携への移行が有力な選択肢の一つになる）。

## 3. 認可エラーの検知・区別方針（設計要件）

GASでGmail連携の認可が切れた場合、「Authorization is required to perform that action」等の特定メッセージを含む例外が発生する。これをtry/catchで検知し、通常の処理エラーとは別種別（例：バッジ「要再認証」）として処理エラーアラート一覧（詳細設計_ダッシュボード.md 9章）に記録する。この不具合（2(b)）は放置しても自然には復旧しないため、通常の一時的なエラーと同列に扱うと見逃されるリスクが高い。**GASを採用する機能は、この検知ロジックを例外処理に必ず組み込むこと。**

## 4. 再認可手順の簡易マニュアル

2(d)で示した再認可手順（script.google.comを開く→関数を実行→認可ダイアログで許可、など）を、スクリーンショット付きの簡易マニュアルとして用意し、ダッシュボードの「要再認証」表示からリンクできるようにする（詳細設計_ダッシュボード.md 9章参照）。GASを採用する機能が複数になっても、マニュアルは1つに集約してよい（再認可の操作自体はどの機能でも同じ手順のため）。

## 5. 監視の仕組み（最終実行日時の記録）

2(b)の不具合に備え、GASを採用する機能は「最終実行日時」を`PropertiesService`等で記録し、想定スキャン間隔を超えて更新がなければ異常とみなしてダッシュボードで検知できる仕組みを、努力目標ではなく必須要件として組み込むこと（処理エラーアラート一覧との連携。詳細設計_ダッシュボード.md 9章）。

この異常判定は、ダッシュボードを開いた（表示した）タイミングで、現在時刻と各機能の最終実行日時を比較して行う（2026-09-16決定）。比較処理自体はサーバー側（Spring Boot）で実行する（ダッシュボードの実装方式確定に伴う決定。詳細は詳細設計_ダッシュボード実装方式.mdを参照）。

## 6. メール本文の外部送信とデータ保持について

GASを使う・使わないに関わらず、本ツールはAI（Claude／Anthropic API）にメール本文の内容を判断させる仕組みのため、GASから`UrlFetchApp`でメール本文を含むデータをAnthropicのAPIに送信することになる。この「メール本文が外部（Anthropic）に送られる」こと自体は、AI判定を行う以上どの機能でも避けられない前提であり、GAS特有の注意点ではないが、GAS運用の一部として押さえておくべき事項のため、ここに記載する（2026-09-15調査）。

Anthropicの公開ポリシー（2026-09時点で確認）は以下の通り：
- **モデル学習への非利用**：API経由で送信したデータは、デフォルトではモデルの学習には使われない（利用者が明示的にフィードバック機能等で同意しない限り）（[Claude Privacy Center](https://privacy.claude.com/en/articles/7996868-is-my-data-used-for-model-training)）
- **データ保持期間**：商用組織向けのAPI利用では、原則として受信から30日以内にデータが自動削除される（法令対応・不正利用調査等の例外を除く）（[Claude Privacy Center](https://privacy.claude.com/en/articles/7996866-how-long-do-you-store-my-organization-s-data)）
- **通信の暗号化**：API通信はHTTPS（暗号化通信）で行われる
- **ゼロデータ保持（ZDR）オプション**：Anthropicの営業窓口に申し込むことで、APIレスポンス返却後にプロンプト・レスポンスを一切保存しない契約（ZDR）を結ぶことも可能（[Claude Platform Docs](https://platform.claude.com/docs/en/manage-claude/api-and-data-retention)）

**フェーズ1（個人利用）では、上記のデフォルトの取り扱い（学習非利用・30日で自動削除・通信暗号化）で実務上問題ない水準と考えられる。**

**将来、企業向けに展開する段階（`メールアシスタントツール_仕様書.md` 8章、BYOK・顧客企業の営業秘密や個人情報を扱う想定）になった場合は、顧客企業とのやり取りの中で、Anthropicとのゼロデータ保持（ZDR）契約や、その他のデータ取り扱いに関する契約上の取り決めが必要になる可能性が高い。**この点は導入検討時に改めて確認すること。

## 7. 適用対象

- **下書き自動生成・日程調整アポイント・特定受信アドレス・迷惑メール誤判定検知の全4機能**：GAS採用済み（2026-09-15決定）。本ドキュメントの内容をすべての機能に適用する。

## 8. 残課題（Open Questions）

- GASの「Gmail読み取り」自体の1日あたり定量的なクオータについて、公式ドキュメント上の明確な数値が確認できなかった。実運用で問題が出ないか、小規模な試験運用で確認する
- GASのトリガー無効化バグ（2(a)）への対処を、V8ランタイム無効化とWebアプリデプロイ経由実行のどちらで行うか（または様子見するか）
- 将来の企業向け展開時、Anthropicとのゼロデータ保持（ZDR）契約等が実際に必要になるか、必要な場合の手続き・コスト（6章参照）
