# 詳細仕様：ダッシュボード実装方式（技術スタック・API仕様）

親仕様：メールアシスタントツール_仕様書.md／詳細設計_ダッシュボード.md
関連：共通基盤_GAS運用の注意点.md（8章「ダッシュボード実装方式が未定」の残課題に対する回答）

作成日: 2026-09-16

## 0. 本ドキュメントの位置づけ

`詳細設計_ダッシュボード.md`が画面構成・表示項目（何を見せるか）を定義しているのに対し、本ドキュメントは「それをどう作るか」（技術スタック・データの流れ・API仕様）を定義する。`共通基盤_GAS運用の注意点.md` 8章および`詳細設計_迷惑メール誤判定検知.md` 8章に残されていた「ダッシュボード自体の実装方式が未定」という残課題への回答でもある（2026-09-16決定）。

## 1. 全体アーキテクチャ（決定事項）

- **フロントエンド**：静的HTML/CSS/JS（グラフはChart.js）。設計を最優先するため、フレームワーク非依存の素のHTML/CSS/JSで作る
- **バックエンド**：Java（Spring Boot）によるREST API
- **データストア**：バックエンドが所有するリレーショナルDB（PostgreSQL推奨。個人利用規模ならSupabase等の無料枠で足りる）
- **既存4機能（下書き自動生成・日程調整アポイント・特定受信アドレス・迷惑メール誤判定検知）**：GAS実装のまま変更しない（`共通基盤_GAS運用の注意点.md`で決定済みの方針を維持。2026-09-18更新：「HP問い合わせ」は`詳細設計_特定受信アドレス.md`側の改称・一般化決定にあわせて「特定受信アドレス」に呼称変更）

### データの流れ（決定事項）

GASとダッシュボードのDBは直接つながず、必ずSpring BootのAPIを経由する。GAS側がDBに直接読み書きする構成にしない理由は、データの整合性チェック・バリデーションをSpring Boot側の1箇所に集約するため。

```
[GAS：4機能]  --(A) HTTPS POST-->  [Spring Boot API]  <-->  [DB (PostgreSQL)]
                                          ^   |
                                          |   +--(B) HTTPS GET/POST/PATCH-->  [ブラウザ：ダッシュボード画面]
                                          |
[GAS：WebアプリとしてdoPost公開]  <--(C) HTTPS POST--+
```

- **(A) 取り込み（ingest）API**：GASの各Stepの最後で、処理結果（下書き作成・保留・注意メッセージ・エラーなど）をSpring Boot APIにPOSTする。12章参照
- **(B) 画面表示・操作API**：ブラウザ（ダッシュボード画面）がSpring Boot APIを呼んで表示データを取得し、ボタン操作（削除・再分類・対応状況変更など）を行う。3〜11章参照
- **(C) 再処理コマンド**：ダッシュボードでの「不要」削除・手動カテゴリ再分類・一時しきい値再処理・AI判定リトライ・迷惑メール受信トレイ復帰などの操作は、Spring Boot単体では完結できない（Gmail・カレンダーの操作はGAS側にしかロジックがないため）。そのためSpring Boot APIは、これらの操作を受けたら**GAS側もWebアプリとして公開し（`doPost`）**、そのURLにHTTPS POSTでコマンドを転送する。13章参照

この構成により、GASは「Gmail/カレンダーを操作する層」、Spring Bootは「データを蓄積・集計してUIに渡す層」という役割分担になる。

### なぜこの構成か（判断理由）

- GASは、迷惑メールフォルダへのアクセスなど本ツールのコア処理に必須（`検証_迷惑メール誤判定検知_実装方式.md`）であり、変更しない前提はすでに確定済み
- ダッシュボードは書き込み操作（削除・再分類・ステータス変更）を伴う「アプリ」であり、GASのHtmlService単体で作るより、Spring Boot＋素のHTML/CSS/JSの方がデザインの自由度が高い（うねのさんの希望：デザイン重視）
- GAS・Spring Bootのどちらも「HTTPで呼び出す・呼び出される」だけの関係にしておけば、将来Spring Boot部分をPython（FastAPI等）に差し替える場合もフロントエンド（HTML/CSS/JS）はそのまま使い回せる

### ホスティングについて（要検討）

GASが(A)でSpring Boot APIを呼び出す都合上、Spring Bootアプリは常時起動し、外部からHTTPSで到達できる場所にデプロイする必要がある（自分のPCで都度起動する運用では、GASからのPOSTを受け取れない）。個人利用規模であれば、無料〜低額のPaaS（Railway、Render、Fly.io等）やGoogle Cloud Run（最小インスタンス数1でスリープさせない設定）が候補になる。この点は15章のOpen Questionsに残す。

## 2. データモデル（DBテーブル概要）

各画面のAPIが返すデータの土台となるテーブル。厳密なDDLは実装時に詰めるとして、ここでは論理的な項目だけ定義する。

- **emails**：受信メールの基本情報を集約する中心テーブル（下書き一覧・保留一覧・注意メッセージ・未分類一覧などが共通して参照する）
  - id, receivedAt, sender, subject, category（typical_inquiry / scheduling / hp_inquiry / specific_address / uncategorized）, judgeMethod（rule / ai）, status（下書き作成済み・未送信 / 予定登録済み・下書き確認待ち / 保留 / 未対応 / 未分類）, draftGmailLink, hasUncertainMarker, originalMailLink
- **pending_reasons**：保留メール一覧の保留理由（emailsに1:1、category=scheduling等が対象）
  - emailId, reason（候補日なし／日時解析不可／判定不明）
- **category_notices**：カテゴリ固有の注意メッセージ
  - id, occurredAt, category, message, originalMailLink, relatedDraftLink
- **specific_address_inquiries**：特定受信アドレス一覧専用テーブル（表示項目・対応状況の軸がemailsと異なるため分離。2026-09-18改称：旧`hp_inquiries`）
  - id, receivedAt, **targetAddress**（実際に一致した特定受信アドレス一覧上のアドレス。例：`info@ysystems.co.jp`、`job@ysystems.co.jp`。カテゴリ別受信件数のアドレス別内訳〔`詳細設計_ダッシュボード.md` 2章参照〕の集計キー）, companyName, contactName, email, phone, summary, type（問合せ／営業／他。2026-09-08改称：旧「本来の問い合わせ／営業PR／その他」）, formCategory, originalMailLink, status（未対応／対応済み／保留）, formatUnrecognizedFlag, processingErrorFlag, draftCreatedFlag
- **spam_review_results**：迷惑メール誤判定検知結果一覧
  - id, detectedAt, senderEmail, senderCompany, senderContact, subject, mailCategory（メルマガ系／広告系／支払系／有効期限系／その他）, aiRetryNeeded, **gmailThreadId**（対象メールのGmail上のスレッドID。受信トレイへ戻す操作〔9章「受信トレイへ戻す」・詳細設計_迷惑メール誤判定検知.md 3章Step4〕で、GAS側が拡張Gmail APIで操作対象を特定するために使用。2026-09-19追加：受信トレイへ戻す操作の実装方式決定に伴う）
- **processing_errors**：処理エラーアラート一覧
  - id, occurredAt, functionName（下書き自動生成／日程調整アポイント／特定受信アドレス／迷惑メール誤判定検知）, result（正常終了／エラー）, errorType（通常／要再認証）, errorDetail, retryable
- **function_last_run**：各機能の最終実行日時（監視用）
  - functionName（主キー）, lastRunAt, expectedIntervalMin
- **settings**：システム設定画面の各種設定値（キー・値のシンプルな構成でよい。11章参照）

## 3. 全体サマリー画面

対応：`詳細設計_ダッシュボード.md` 2章

```
GET /api/summary?from=2026-08-01&to=2026-08-31
```

```json
{
  "period": { "from": "2026-08-01", "to": "2026-08-31" },
  "pie": {
    "totalReceived": 320,
    "spamCount": 45,
    "draftGeneratedCount": 79,
    "specificAddressCount": 13,
    "misclassifiedFoundCount": 3,
    "categoryBreakdown": {
      "typicalInquiry": 20, "scheduling": 15,
      "specificAddress": {
        "total": 13,
        "byAddress": { "job@ysystems.co.jp": 10, "info@ysystems.co.jp": 3 },
        "other": 0
      },
      "spamReview": 8, "uncategorized": 11
    },
    "pendingCount": 5
  },
  "draftAgingBarChart": [
    { "days": 1, "count": 20 }, { "days": 2, "count": 14 },
    { "days": 3, "count": 7 }, { "days": 4, "count": 1 }, { "daysOrMore": 5, "count": 10 }
  ]
}
```

なお、下書き修正率のゲージ表示、および受信メールのグループ化・フォルダ表示機能は実装しない（2026-09-16決定。`詳細設計_ダッシュボード.md`側も本決定にあわせて修正済み）。本APIにもこの2つに対応するエンドポイントは含めていない。また、`categoryBreakdown`の内訳から社外／社内（`externalCount`／`internalCount`）の区分を廃止し、迷惑メール誤判定検知（`spamReview`）を追加した（2026-09-17決定。`詳細設計_ダッシュボード.md` 2章参照）。`categoryBreakdown`の各値はカテゴリ別受信件数の円弧グラフに対応し、クリックすると対応する一覧画面（4・8・9章の各API参照）に遷移する（2026-09-17決定）。`categoryBreakdown.specificAddress`は、システム設定画面に登録された宛先アドレスごとの内訳（`byAddress`）を持つ。`byAddress`はSpring Boot側で受信件数の多い順にソートし、上位3件のみを返す。登録アドレスが4件以上ある場合、4件目以降の合計を`other`に格納する（`other`が0なら該当なし）。画面上はアドレスごとに個別の弧として表示し、`other`があれば「その他」として1本追加する（2026-09-18決定。`詳細設計_ダッシュボード.md` 2章・7章参照）。各弧のクリックは`GET /api/specific-address?targetAddress=info@ysystems.co.jp`のように絞り込みクエリで8章の一覧APIを呼び出す（`other`の弧をクリックした場合の絞り込み方法は別途検討）。`draftAgingBarChart`の最終要素は`days`ではなく`daysOrMore`キーとし、その日数以上経過した下書きをすべて合算した値を返す（2026-09-18決定。`詳細設計_ダッシュボード.md` 2章参照）。

## 4. 要確認の下書き一覧

対応：`詳細設計_ダッシュボード.md` 3章

```
GET /api/drafts?status=unconfirmed
```
```json
[
  {
    "id": "em_00123",
    "receivedAt": "2026-08-29T09:14:00+09:00",
    "sender": "tanaka@example.co.jp",
    "subject": "資料送付のお願い",
    "category": "typical_inquiry",
    "judgeMethod": "ai",
    "draftGmailLink": "https://mail.google.com/mail/u/0/#drafts/xxxx",
    "hasUncertainMarker": true,
    "schedulingAutoBookedFlag": false
  }
]
```

- 「不要」操作：`DELETE /api/drafts/{id}` → Spring Boot内のレコード削除に加えて、13章の再処理コマンド（`deleteDraft`）経由でGAS側のGmail下書きも必ず削除する（ダッシュボードの管理データのみを消す方式は採らない。2026-09-17決定。`詳細設計_ダッシュボード.md` 3章参照）
- 手動カテゴリ再分類：`PATCH /api/drafts/{id}` `{ "newCategory": "scheduling" }` → 13章の再処理コマンドでGASに転送し、Step3以降を再実行させる

## 5. 保留メール一覧

対応：`詳細設計_ダッシュボード.md` 4章

```
GET /api/pending-emails
```
```json
[
  {
    "id": "em_00456",
    "receivedAt": "2026-08-27T11:40:00+09:00",
    "sender": "suzuki@example.co.jp",
    "subject": "打ち合わせ日程について",
    "category": "scheduling",
    "pendingReason": "日程調整・日時解析不可",
    "originalMailLink": "https://mail.google.com/mail/u/0/#inbox/xxxx"
  }
]
```

## 6. カテゴリ固有の注意メッセージ

対応：`詳細設計_ダッシュボード.md` 5章

```
GET /api/notices
```
```json
[
  {
    "id": "nt_00789",
    "occurredAt": "2026-08-29T09:14:00+09:00",
    "category": "scheduling",
    "message": "相手提示日（8/30 14:00）に予定を自動登録しました",
    "originalMailLink": "https://mail.google.com/mail/u/0/#inbox/xxxx",
    "relatedDraftLink": "https://mail.google.com/mail/u/0/#drafts/xxxx"
  }
]
```

画面側の仕様（既に決定済み）：一覧の行をダブルクリックすると元メール・関連下書きが開く（行自体がリンク）。個別の列としての「リンク」表示は設けない。

## 7. 手動カテゴリ再分類・一時しきい値再処理

対応：`詳細設計_ダッシュボード.md` 6章、`詳細設計_下書き自動生成.md` 3章Step2

- メール単位のカテゴリ変更：4章のPATCH `/api/drafts/{id}`、5章相当のPATCH `/api/pending-emails/{id}` で共通提供（対象カテゴリの全機能で共通利用）
- 定型的な問い合わせ対応の一時しきい値再処理：
```
POST /api/typical-inquiry/reprocess
{ "temporaryThreshold": 0.4, "from": "2026-08-01", "to": "2026-08-31" }
```
```json
{ "promotedCount": 3, "demotedCount": 1 }
```
この操作はStep2の再比較のみ（AI再判定なし）なので、Spring Boot側で保存済みの確信度と再比較して即座に結果を返せる。ただし実際に下書きを作成・削除する処理はGAS側にしかロジックがないため、内部的には13章の再処理コマンドでGASに転送する。

このAPIの呼び出し元（画面側の入口）は、共通ヘッダーの「問合せ判定閾値」入力欄とする（2026-09-18決定：当初はシステム設定画面の機能とする想定だったが、共通ヘッダーへの常設に変更。`詳細設計_ダッシュボード.md` 1章・6章参照）。

## 8. 特定受信アドレス一覧

対応：`詳細設計_ダッシュボード.md` 7章、`詳細設計_特定受信アドレス.md` 4章Step5・Step6（2026-09-18改称：旧「HP問い合わせ一覧」／`詳細設計_HP問い合わせ.md`。特定受信アドレス.md自体の改称・一般化決定に伴う）

```
GET /api/specific-address?type=問合せ&targetAddress=info@ysystems.co.jp
```
```json
[
  {
    "id": "sa_00321",
    "receivedAt": "2026-08-20T10:00:00+09:00",
    "targetAddress": "info@ysystems.co.jp",
    "companyName": "株式会社サンプル",
    "contactName": "山田太郎",
    "email": "yamada@sample.co.jp",
    "phone": "090-xxxx-xxxx",
    "summary": "製品Aの見積を依頼したい",
    "type": "問合せ",
    "formCategory": "見積依頼",
    "originalMailLink": "https://mail.google.com/mail/u/0/#inbox/xxxx",
    "status": "未対応",
    "formatUnrecognizedFlag": false,
    "processingErrorFlag": false,
    "draftCreatedFlag": false
  }
]
```

`targetAddress`クエリパラメータは、全体サマリー（2章）のアドレス別の弧をクリックした際の絞り込みに使う（任意パラメータ。省略時は全アドレス横断で返す。2026-09-18決定）。

- 対応状況変更：`PATCH /api/specific-address/{id}/status` `{ "status": "対応済み" }`
- 種別の手動訂正：`PATCH /api/specific-address/{id}/type` `{ "type": "営業" }`
- 返信下書きワンクリック作成（Step6）：`POST /api/specific-address/{id}/draft` → 13章経由でGASに転送し、新規メール下書き（宛先・結び・署名のみ）を作成させる

## 9. 迷惑メール誤判定検知 結果一覧

対応：`詳細設計_ダッシュボード.md` 8章、`詳細設計_迷惑メール誤判定検知.md` 3〜4章

```
GET /api/spam-review
```
```json
[
  {
    "id": "sp_00654",
    "detectedAt": "2026-08-26T03:12:00+09:00",
    "senderEmail": "info@partner.co.jp",
    "senderCompany": "パートナー株式会社",
    "senderContact": "佐藤様",
    "subject": "お見積りの件",
    "mailCategory": "その他"
  }
]
```

- 受信トレイへ戻す：`POST /api/spam-review/{id}/restore` → 13章経由でGASに転送（コマンド`restoreFromSpam`。GAS側は拡張Gmail API（Advanced Gmail Service）で、`gmailThreadId`〔2章参照〕を対象に`SPAM`ラベルを外し`INBOX`ラベルを付与する。2026-09-19決定。詳細設計_迷惑メール誤判定検知.md 3章Step4・8章参照）
- AI判定エラー時の個別リトライ：`POST /api/spam-review/{id}/retry`

## 10. 処理エラーアラート一覧

対応：`詳細設計_ダッシュボード.md` 9章

```
GET /api/system/last-run
```
```json
[
  { "functionName": "下書き自動生成", "lastRunAt": "2026-09-16T09:05:00+09:00", "expectedIntervalMin": 5, "isStale": false },
  { "functionName": "迷惑メール誤判定検知", "lastRunAt": "2026-09-16T08:40:00+09:00", "expectedIntervalMin": 5, "isStale": true }
]
```
`isStale`（現在時刻－lastRunAt > expectedIntervalMin）の判定はSpring Boot側（サーバー側）で行う。ブラウザ側での計算にしない理由は、複数端末から見ても判定結果が一致するようにするため（`共通基盤_GAS運用の注意点.md` 8章のOpen Questionへの回答）。

```
GET /api/errors
```
```json
[
  {
    "id": "er_00987",
    "occurredAt": "2026-08-26T03:12:00+09:00",
    "functionName": "特定受信アドレス",
    "result": "エラー",
    "errorType": "通常",
    "errorDetail": "構造化項目の抽出に失敗しました",
    "retryable": false
  },
  {
    "id": "er_00988",
    "occurredAt": "2026-08-25T00:00:00+09:00",
    "functionName": "日程調整アポイント",
    "result": "エラー",
    "errorType": "要再認証",
    "errorDetail": "Authorization is required to perform that action",
    "retryable": false,
    "reauthManualLink": "/manuals/oauth-reauth.html"
  }
]
```

- 共通ヘッダーのOAuthバッジ：`GET /api/system/oauth-status` → `{ "status": "ok" | "error" }`（`errorType: 要再認証`のレコードが1件でも存在すれば`error`）
- 個別リトライ：`POST /api/errors/{id}/retry`（AI判定エラーのみ`retryable: true`）

処理エラー発生件数ランキング（機能別、2026-09-18追加。`詳細設計_ダッシュボード.md` 9章参照）：

```
GET /api/errors/ranking
```
```json
[
  { "functionName": "特定受信アドレス", "errorCount": 6 },
  { "functionName": "下書き自動生成", "errorCount": 4 },
  { "functionName": "日程調整アポイント", "errorCount": 2 }
]
```
Spring Boot側で機能別に集計し、件数降順で上位3件のみ返す（4位以下は返さない。2026-09-18決定）。集計対象期間は`GET /api/errors`と同じ。

## 11. システム設定画面

対応：`詳細設計_ダッシュボード.md` 11章

```
GET /api/settings
```
```json
{
  "scheduling": {
    "workingDays": ["MON","TUE","WED","THU","FRI"],
    "workingHours": { "start": "09:00", "end": "18:00" },
    "meetingDurationMin": 60,
    "candidateSearchRangeWeeks": 2,
    "keywords": ["ご都合のよい日程", "打ち合わせ", "面談"]
  },
  "specificAddress": {
    "targetAddresses": ["info@ysystems.co.jp", "job@ysystems.co.jp"],
    "labelAliases": { "担当者名": ["お名前", "氏名", "ご担当者名"] }
  },
  "typicalInquiry": {
    "likelyFeatures": ["初めての相手からの単発質問"],
    "unlikelyFeatures": ["進行中の案件の続き"],
    "threshold": 0.5
  },
  "spamScan": { "intervalMin": 5 },
  "logRetentionDays": 30,
  "dashboardRefreshIntervalMin": 5,
  "categoryColors": {
    "typicalInquiry": "#f5a623", "scheduling": "#4a90d9", "specificAddress": "#7ed321",
    "spamReview": "#d0021b", "uncategorized": "#9013fe", "pending": "#50e3c2"
  }
}
```
更新は項目のまとまりごとに分ける：`PUT /api/settings/scheduling`、`PUT /api/settings/specific-address`、`PUT /api/settings/typical-inquiry`、`PUT /api/settings/spam-scan`、`PUT /api/settings/log-retention`、`PUT /api/settings/dashboard-refresh`、`PUT /api/settings/category-colors`（2026-09-18追加：ログ保存期間・ダッシュボード自動更新間隔・カテゴリ表示色の設定項目。`詳細設計_ダッシュボード.md` 11章・13章「決定済み事項」参照）。`specificAddress`は2026-09-18改称（旧`hpInquiry`）で、`targetAddresses`は複数登録可能（例のinfo@／job@は全体サマリーのアドレス別リング表示に対応）。`categoryColors`はカテゴリ別受信件数の円グラフ・円弧グラフ・コンボチャート共通の配色に使う（キーは`categoryBreakdown`の区分名と対応。2026-09-18決定。`詳細設計_ダッシュボード.md` 2章参照）。

## 12. GAS→Spring Boot 取り込みAPI（ingest）

GASの各機能が、各Stepの最後にこれらのエンドポイントへPOSTしてSpring Boot側のDBを更新する。ブラウザ向けAPI（3〜11章）とは別の名前空間（`/ingest/*`）に分け、認証はAPIキー（共有シークレット）をヘッダーに付与する方式とする（14章）。

| エンドポイント | 呼び出し元（機能・Step） |
|---|---|
| `POST /ingest/emails` | 下書き自動生成 Step5／日程調整アポイント Step5（下書き作成・ステータス更新のたび） |
| `POST /ingest/notices` | 日程調整アポイント Step4-A・4-B（注意メッセージ発生時） |
| `POST /ingest/specific-address` | 特定受信アドレス Step5（2026-09-18改称：旧`/ingest/hp-inquiries`） |
| `POST /ingest/spam-review` | 迷惑メール誤判定検知 Step3（`gmailThreadId`〔2章参照〕を含めて送信する。2026-09-19決定：受信トレイへ戻す操作の実装に必要なため） |
| `POST /ingest/errors` | 4機能共通、例外処理の都度 |
| `POST /ingest/last-run` | 4機能共通、Step1の最後（正常終了・エラーどちらでも記録。`詳細設計_迷惑メール誤判定検知.md` 3章Step1参照） |

## 13. Spring Boot→GAS 再処理コマンドAPI

Spring Boot側は、ブラウザからの操作（4・7・8・9章の各POST/PATCH/DELETE）を受けたら、DBの更新に加えて、Gmail・カレンダーの実操作が必要なものはGASのWebアプリ（`doPost`で公開）にコマンドを転送する。

```
POST https://script.google.com/macros/s/{デプロイID}/exec
{
  "command": "reclassify",
  "emailId": "em_00123",
  "newCategory": "scheduling",
  "apiKey": "（共有シークレット）"
}
```

想定コマンド：`reclassify`（手動カテゴリ再分類）、`deleteDraft`（下書き削除）、`reprocessThreshold`（一時しきい値再処理）、`createSpecificAddressDraft`（特定受信アドレス下書き作成。2026-09-18改称：旧`createHpInquiryDraft`）、`restoreFromSpam`（迷惑メール復帰。パラメータに`gmailThreadId`を含む。GAS側は拡張Gmail APIで`SPAM`ラベルを外し`INBOX`ラベルを付与する。2026-09-19決定）、`retryAiJudgement`（AI判定リトライ）。GAS側はこのコマンドを受けて該当Stepの処理を実行し、完了後に12章のingest APIで結果をSpring Boot側に反映する。

## 14. 認証方式（暫定案）

- **ブラウザ↔Spring Boot（3〜11章のAPI）**：フェーズ1は利用者本人のみのため、Spring Security＋固定の1アカウントによるログイン（Basic認証でも可）で十分。将来の企業向け展開時にマルチテナント対応のログイン方式に置き換える
- **GAS↔Spring Boot（12・13章）**：人が操作するものではないため、共有シークレットのAPIキーをヘッダー（例：`X-API-Key`）に付与する方式とする。GAS側のWebアプリは「全員」に公開しつつ、doPost内でAPIキーを検証して弾く形にする（GASのWebアプリ自体をGoogleアカウント認証だけで守る設定は、外部からのPOSTを受け付けられなくなるため不採用）

## 15. Open Questions（本ドキュメント固有）

- Spring Bootアプリのホスティング先（1章「ホスティングについて」参照。GASからの常時到達性が必要）
- DBのホスティング先（自前運用かSupabase等のマネージドサービスか）
- GASのWebアプリ公開設定（実行ユーザー：自分／アクセスできるユーザー：全員、で設定した場合のセキュリティ上の注意点の洗い出し）

### 決定済み事項（参考）
- **「不要」操作でのGmail下書き削除要否**：Gmail側の下書きも必ず削除する。`deleteDraft`コマンドを使用する（2026-09-17決定。`詳細設計_ダッシュボード.md` 13章「決定済み事項」参照）
- **「HP問い合わせ」→「特定受信アドレス」への呼称統一**：`詳細設計_特定受信アドレス.md`が「HP問い合わせ」から「特定受信アドレス」へ改称・一般化された決定を受け、本ドキュメントのテーブル名・APIパス・設定キー・コマンド名も同様に統一した（2026-09-18決定。2・8・11・12・13章参照）
- **迷惑メール誤判定検知の「受信トレイへ戻す」操作のGAS側実装方式**：拡張Gmail API（Advanced Gmail Service）で、`gmailThreadId`を対象に`SPAM`ラベルを外し`INBOX`ラベルを付与する（2026-09-19決定。2・9・12・13章参照。詳細設計_迷惑メール誤判定検知.md 3章Step4・8章参照）
