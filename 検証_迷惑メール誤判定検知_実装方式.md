# 調査メモ：迷惑メール誤判定検知機能の実装方式（Gmailコネクタ再検証／GAS実装の妥当性）

作成日: 2026-09-09
親仕様：メールアシスタントツール_仕様書.md 6章「迷惑メール誤判定検知機能（案）」
関連：詳細設計_ダッシュボード.md 8章・11章・13章／共通基盤_GAS運用の注意点.md（GAS共通の運用注意点はこちらに分離。本ドキュメントは本機能固有の内容のみ扱う）／詳細設計_迷惑メール誤判定検知.md（本調査結果をもとにした機能仕様レベルの詳細設計）

本ドキュメントは、迷惑メール誤判定検知機能の詳細設計に着手する前段階として、(1) Gmailコネクタでスパムフォルダに本当にアクセスできないのかの再検証、(2) GASでの実装がコスト・安定性の面で妥当かどうかの検討材料をまとめたもの。詳細設計そのものはまだ行わない。

## 1. 結論サマリー

- **Gmailコネクタ経由でのスパムフォルダアクセスは、今回改めて検証しても不可能**という結論に変わりなし。以前の検証結果（誤判定なし）を再確認した。
- **GASでのスパムフォルダアクセスは技術的に確立された方法**であり、複数の実例（個人ブログ・note記事）が存在する。`GmailApp.search('in:spam ...')`で問題なく取得できる。
- 一方で、**Gmail API自体（GASを介さない生のAPI）にもスパムへのアクセス手段は用意されている**（`labelIds: ["SPAM"]`、`includeSpamTrash`パラメータ）。つまり「スパムにアクセスできない」のはGmail APIの制約ではなく、**Claude用Gmailコネクタ（MCPサーバー）側が意図的に絞っている制限**である可能性が高い（安全性配慮によるものと推測されるが、公式な説明は見当たらなかった）。
- 個人利用・フェーズ1の規模（スパム221件程度、1日1〜数回のスキャン）であれば、**GASのクオータ（上限枠）は十分に余裕がある**。前回チェック時刻以降の新着分のみを処理する差分方式を前提とすると、1日の想定処理件数（多くて数十件）に対し、実行時間上限（6分/回・90分/日）にも外部API呼び出し上限（20,000回/日）にも遠く届かない（詳細な試算は3.2参照。クオータの数値自体は`共通基盤_GAS運用の注意点.md` 1章参照）。ただし、**導入初回に既存の221件を一括処理する場合は6分の壁に接近しうる**ため、初回のみ分割実行を検討する。GASを軸に進める方針は妥当と判断する。
- なお、GASには運用上の不安定要素が2種類ある（トリガー無効化バグ・認可エラーによる停止）。これらは本機能に限らずGASを使う機能全般に共通する内容のため、`共通基盤_GAS運用の注意点.md`にまとめた（3.3参照）。
- 将来、企業向け展開（BYOK・マルチテナント、仕様書8章）を見据えて本格的なサーバー化をする段階になれば、GASではなくGmail API＋自前バックエンド（Cloud Functions/Cloud Run等）への移行を検討する余地がある。ただしフェーズ1（個人利用）では過剰投資と判断する。

## 2. 検証1：Gmailコネクタでのスパムアクセス再検証（2026-09-09実施）

以下のクエリをClaude用Gmailコネクタ（`search_threads`）で実行し、いずれも結果0件（`{}`）だった。

| クエリ | 結果 |
|---|---|
| `in:spam` | 0件 |
| `is:spam` | 0件 |
| `label:SPAM` | 0件 |
| `in:anywhere label:SPAM` | 0件 |
| `from:(スパム内送信者) label:spam` | 0件 |

一方、`list_labels`ではSPAMラベルに**221件**のメッセージが存在することを確認済み（既知の事実と一致）。

さらに、ツールの説明文には「`in:anywhere` — Search in all folders (**including spam and trash**)」と明記されているにもかかわらず、`in:anywhere`で50件取得した中に**SPAMラベル付きメッセージは1件も含まれていなかった**（TRASHラベル付きは複数含まれており、TRASHへのアクセス自体は可能）。このことから、`in:anywhere`のドキュメント上の説明と実際の挙動が一致しておらず、**SPAMだけが特別に除外されている**ことが明確になった（TRASHは除外されていない）。

以上により、前回の検証結果（Gmailコネクタ経由でのスパムフォルダアクセスは不可能）は**再確認・裏付けされた**。ツールの実装上の意図的な制限と考えるのが妥当（フィッシング・詐欺メールを誤ってAIに読ませない、またはAIが自動操作することへの安全側の配慮と推測されるが、Anthropic公式の説明文書は見当たらなかった）。

## 3. 検証2：GASでのスパムフォルダアクセス

### 3.1 技術的な実現性

`GmailApp.search()`でのスパムフォルダアクセスは、複数の実例で確認できた。

- `GmailApp.search('is:unread in:spam after:... before:...', 0, 25)` で直近の未読スパムを取得し、外部AI（OpenAI API）にスコアリングさせる実装例（[labnol.org](https://www.labnol.org/internet/email/prevent-gmail-from-marking-spam-emails/4030)）。まさに本機能と同じ発想（「正規メールらしいものを検知して知らせる」）を先行実装している事例。
- `GmailApp.search('in:spam', 0, 20)` で直近20件のスパムを取得し、送信元・件名を通知する実装例（[note.com](https://note.com/eiichiro49/n/n946cc014fca4)）。
- `GmailApp.search('in:spam newer_than:30d')` ＋ `GmailApp.getMessagesForThreads()` で過去30日分のスパムを取得し、`PropertiesService`で前回チェック時刻を保持して重複通知を避ける実装例（[note.com](https://note.com/freeelover/n/nddbcbdea849b)）。本機能の「定期スキャン」「差分検知」の参考実装として有用。

なお、上記のnote記事の1つに「GASはWorkspaceユーザーのみ利用可能」という記述があったが、これは誤り（不正確な情報）。個人のGoogleアカウント（無料のgmail.com等）でも[script.google.com](https://script.google.com)からGASは無料で利用できる。うねのさんのGmailアカウントも個人アカウント想定のため、consumer（個人向け）のクオータが適用される前提で以下を整理する。

### 3.2 クオータ・制限：本機能への当てはめ（試算）

GASのクオータ・制限の一般的な数値（consumer個人アカウント基準の実行時間・トリガー数・API呼び出し回数など）は、本機能に限らずGASを使う機能全般に共通するため`共通基盤_GAS運用の注意点.md` 1章にまとめた。ここでは、その数値を本機能（迷惑メール誤判定検知）の想定処理量に当てはめた試算のみを記載する。

前提として、本機能は「前回チェック時刻以降の新着スパムのみを対象にする差分方式」とする（毎回221件全件を読み直さない）。

- **実行時間（6分/回の壁）**：1件あたりの処理（本文取得＋Claude API呼び出し＋結果保存）を仮に2〜3秒とすると、1回のスキャンで新着スパムが数件〜数十件程度なら1分もかからず終わる。6分に接近するのは「1回のスキャンで100件以上をまとめて処理しようとしたとき」で、通常運用（差分方式・1日数回スキャン）では起きにくい。**例外は導入初回**：既存の221件を最初に一括チェックする場合、2秒×221件≒約7分となり6分を超える可能性がある。初回のみバッチを分割する（例：100件ずつ複数回のトリガー実行に分ける）等の対応を詳細設計に含めるべき。
- **1日の合計実行時間（90分/日の壁）**：1日3回スキャンし毎回1分程度なら合計3分。90分の枠に対してはほぼ使い切りようがない。
- **外部API呼び出し回数（20,000回/日の壁）**：1件のスパムにつきAI呼び出し1回だとして、1日数十件処理しても数十回。20,000回の枠に対しては誤差レベル。
- 通知を「ダッシュボード一覧＋Slack」で行う想定（詳細設計_ダッシュボード.md 8章）であり、GAS単体からのメール送信（MailApp）を主な通知手段にしない前提であれば、送信数クオータ（100通/日）も問題にならない。

### 3.3 安定性・運用上の注意点

GASのトリガー不安定性（トリガー無効化バグ・原因不明の認可エラーによる停止・業界共通課題としての位置づけ・再認可の具体的な手順と難易度）は、本機能に限らずGASを使う機能全般に共通する内容のため、`共通基盤_GAS運用の注意点.md` 2章にまとめた。

本機能に固有の補足は以下の通り。

- 本機能はもともと「時間主導型トリガーを1つ設定して定期実行する」だけの単純な構成を想定しているため、共通基盤_GAS運用の注意点.md 2(a)で述べた「トリガー内で新しいトリガーを作成する」パターンには該当しない見込み。
- **本番運用の実績情報が薄い**：調査した範囲の記事はいずれも個人ブログ・note記事・コミュニティフォーラムレベルの情報であり、「本番運用でどの程度の頻度・影響度で発生するか」の定量的な実績データは見つからなかった。うねのさん自身がYsystems向けにGAS採用の求人管理システムを既に運用している実績（/areas/jobs-management-system.md）があるため、その経験値（実際にこの種の停止に遭遇したことがあるか）を踏まえて判断するのが実務的には最も確実。

### 3.4 コスト

- GAS自体の利用は無料（GCPプロジェクトの新規作成や課金設定は不要）。
- 外部AI呼び出しコストは、ホスティング方式（GAS／自前サーバー）に関わらず同額（トークン課金のため）。仕様書7章の試算を流用可能：Claude Haiku 4.5で約0.7円/件、Claude Sonnet 4.6で約2円/件（入力2,000トークン・出力500トークン/件と仮定）。
- スパムメールは本文が短く定型的なものが多いと想定されるため、実際のトークン消費は上記試算より少なくなる可能性がある（実測は別途必要）。

## 4. 検証3：代替方式（Gmail API直接連携）の検討

### 4.1 技術的な実現性

Gmail API自体（GASを介さない生のREST API）には、スパムへのアクセス手段が公式に用意されている。

- `users.messages.list`の`labelIds`パラメータに`["SPAM"]`を指定することでスパムメッセージの一覧取得が可能（[Gmail API公式ドキュメント](https://developers.google.com/workspace/gmail/api/guides/list-messages)）。
- `includeSpamTrash`パラメータをtrueにすることで、通常のクエリ実行時にも`SPAM`/`TRASH`を含めて検索できる。

つまり、**「スパムにアクセスできない」のはGmail API自体の制約ではなく、Claude用Gmailコネクタ（MCPサーバー）が意図的に絞っている制限**であることが、この検証からも裏付けられる。技術的には、OAuth認可を自前で行い、Gmail APIを直接叩く実装（Node.js/Python等でサーバーサイド実装）を組めば、スパムフォルダへのアクセスは何ら問題なく行える。

### 4.2 GASとの比較

| 観点 | GAS（GmailApp経由） | Gmail API直接＋自前サーバー |
|---|---|---|
| 初期構築の手間 | 小（Googleアカウントがあればすぐ書ける。OAuth同意画面の設定不要） | 大（GCPプロジェクト作成、OAuth同意画面設定、クライアントシークレット管理、リフレッシュトークンの永続化・失効対応が必要） |
| ホスティング費用 | 無料（Googleのスクリプト実行基盤を利用） | 実行環境が別途必要（Cloud Functions/Cloud Run等）。個人利用規模なら無料枠内に収まる可能性が高いが、GCPの課金設定（クレジットカード登録）自体は必要になることが多い |
| 実行時間・頻度の制約 | 6分/回、90分/日（個人アカウント） | 制約なし（自前サーバーの設定次第。常駐サーバーならリアルタイム処理も可能） |
| 安定性 | Google依存の既知の不具合あり（共通基盤_GAS運用の注意点.md 2章参照）。個人開発では監視の仕組みが必須 | 自前で監視・アラートを作り込む必要があるが、原理的にはより制御しやすい |
| Gmail新着検知の即時性 | 基本はポーリング（時間主導型トリガー）。Pub/Sub連携でプッシュ通知も可能だが構築コストが高い | Gmail APIの`watch()`＋Cloud Pub/Subでプッシュ通知（ほぼリアルタイム）も可能。ただし本機能（定期スキャンでよいとされている。仕様書6章）には過剰 |
| 開発・保守の担い手 | うねのさん自身がGASでの開発実績あり（Ysystems向け求人管理システム等） | サーバー運用・OAuthトークン管理など新規に習得・保守が必要な要素が増える |
| 将来の企業向け展開（仕様書8章）との整合性 | 個人利用の枠を超えるとGASのクオータ・マルチテナント非対応がボトルネックになりうる | BYOK・マルチテナント化（複数顧客のGoogleアカウントを扱う）を見据えると、最終的にはこちらの構成が必要になる可能性が高い |

### 4.3 所見

フェーズ1（個人利用、スパム221件程度、通知ベースの軽量な機能）においては、**GASの方が明らかに構築・運用コストが低く、うねのさんの既存スキル・既存システム（Ysystems向けGASシステム）とも親和性が高い**。Gmail API直接連携は、技術的な実現性はあるものの、フェーズ1の規模に対しては投資が過剰（OAuth基盤・サーバー運用・課金設定など、機能の価値に対してオーバースペック）と考えられる。

一方で、仕様書8章に記載の「将来の企業向け展開（BYOK・マルチテナント）」を見据えると、本ツール全体（本機能に限らず下書き自動生成等も含む）のアーキテクチャをいずれサーバーサイド化する必要が生じる可能性が高い。その段階になれば、迷惑メール誤判定検知機能もGASから同じサーバー基盤に統合するのが自然。**今はGASでフェーズ1を素早く検証し、企業向け展開が具体化した時点でGmail API直接連携への作り直しを検討する、という段階的な方針が妥当**と考える。

## 5. 推奨方針（案）

1. 迷惑メール誤判定検知機能は**GASベースで実装する方針を正式決定**する（仕様書6章の「要再検討」注記を「決定」に更新）。
2. GASの安定性リスク（共通基盤_GAS運用の注意点.md 2章）に備え、詳細設計時に以下を**必須要件として**組み込む（同ドキュメント2(b)の「自然には復旧しない」不具合への対策として、努力目標ではなく必須とする）：
   - 前回チェック時刻を`PropertiesService`等で保持し、差分のみを対象にする（実行時間の節約、重複通知の防止）
   - 実行のたびに「最終実行日時」を記録し、処理エラーアラート一覧（詳細設計_ダッシュボード.md 9章）で異常検知できるようにする（想定スキャン間隔を超えて更新がなければ異常表示。共通基盤_GAS運用の注意点.md 5章参照）
   - 導入初回のみ、既存221件の一括処理を分割実行する（3.2参照）
   - **認可エラーの検知・区別**：GASでGmail連携の認可が切れた場合に発生する例外をtry/catchで検知し、通常の処理エラーとは別種別（例：バッジ「要再認証」）として処理エラーアラート一覧に記録する（共通基盤_GAS運用の注意点.md 3章参照）。この方針は本機能に限らず、GASでGmail連携を使う機能全般に共通する設計であり、詳細な検知ロジックは`共通基盤_GAS運用の注意点.md`、方針決定の経緯は`メールアシスタントツール_仕様書.md` 7章に記載する
   - **再認可手順の簡易マニュアル**：共通基盤_GAS運用の注意点.md 2(d)・4章で示した再認可手順を、スクリーンショット付きの簡易メモとして用意し、ダッシュボードの「要再認証」表示からリンクできるようにする（詳細設計_ダッシュボード.md側に表示仕様を記載）
3. Claude API（Anthropic）呼び出しは`UrlFetchApp`で行う想定とし、既存の下書き自動生成機能等と判定プロンプトの設計方針を揃える。
4. 本ツール全体のGmail新着検知方式は、今回の調査結果（GASの制約・個人利用規模での実現性）を踏まえ、下書き自動生成・日程調整アポイント・特定受信アドレス・迷惑メール誤判定検知の全カテゴリでGAS（ポーリング方式）に統一する方針を正式決定した（2026-09-15決定。`メールアシスタントツール_仕様書.md` 4章・7章参照）。

## 6. 残課題（Open Questions）

- スパムメール1件あたりの実際のトークン消費量・コストの実測（3.4参照。本文が短い想定だが要検証）
- GAS共通の運用面の残課題（クオータの実測、トリガー無効化バグへの対処方針など）は`共通基盤_GAS運用の注意点.md` 8章参照
