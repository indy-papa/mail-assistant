# メールアシスタントツール 仕様(たたき台) v0.25

作成日: 2026-08-16

## 1. 背景・目的

受信したメールに対して、ある程度AIが内容を判断し、返信の下書きやメール分類を行う仕組みを作る。目的は、定型的なやり取りにかかる対応工数の削減と、返信までのリードタイム短縮。

将来的には全自動送信も視野に入れるが、まずはAIが返信案を作成し、最終確認・送信は人が行う運用（下書き生成）から開始する。

### 位置づけ・将来展望
現段階では個人の業務効率化のための利用を想定する（どこまでAIが実用的な仕組みを作れるか、まず自分で試して見極める段階）。

将来的には、企業向けに販売できる製品への発展も視野に入れる。

## 2. スコープ

### フェーズ1: Gmail限定
- 対象は自分のGmail受信トレイ
- Gmail連携で仕組みが安定してから、Outlook / Microsoft 365など他サービスへの展開を検討する

### 対象とするメールの種類（絞り込み前・検討中）
以下の4種類を候補として、仕様検討・下書き生成のロジックを考える。

1. 定型的な問い合わせ対応（FAQ的な質問、資料送付依頼など）
   - **下書き自動生成（コア機能）の詳細仕様（機能仕様レベル）を別紙にまとめた：`詳細設計_下書き自動生成.md`**
2. 営業・取引先とのやり取り
   - **保留**（2026-08-29判断。利用者が現在無職で具体的な取引先・業界が定まっておらず、今作り込むと特定の想定に寄りすぎるリスクがあるため、当面は詳細設計を見送る。転職活動の状況が固まり具体的な取引先像が見えてきた段階で改めて検討する）
3. 日程調整・アポイント
   - 相手側に候補日を知らせる返信メールの場合、Googleカレンダーを確認して、予定の入っていない日時を返信メールに候補日として記載する
   - 相手が候補日を連絡してきている場合、Googleカレンダーを確認し、予定の入っていない候補日があればその日時で予定を入れる。この場合、自動で予定を入れたことをダッシュボードに注意メッセージとして表示する
   - 実現にはGoogleカレンダーとの連携が別途必要（Gmailコネクタとは別に、Google Calendarコネクタの接続が必要になる想定）
   - **詳細仕様（機能仕様レベル）を別紙にまとめた：`詳細設計_日程調整アポイント.md`**
4. 特定受信アドレス宛のメール（システム設定画面に登録した宛先アドレスに一致するメール。HPのお問い合わせ窓口はその代表例）
   - **詳細仕様（機能仕様レベル）を別紙にまとめた：`詳細設計_特定受信アドレス.md`**（2026-09改称・一般化：旧「HPのお問い合わせページ経由のメール」から改称。宛先アドレスの受信状況を一覧化・可視化する付随機能も本カテゴリに統合済み）

### 分類結果の扱い
- 上記1〜4のいずれにも該当しないメールは「未分類メール」として扱う（下書き生成は行わず、通常の受信メールのまま残す）
- 1〜4それぞれに該当したメール件数、および未分類メールの件数は、ダッシュボードの全体サマリーに集計・表示する（5章参照）
- カテゴリ判定を行う各機能（下書き自動生成など、メールを分類してそれぞれの処理を行う機能）の詳細設計には、この「未分類メールの扱い」と「ダッシュボードへの件数反映」を必ず記載すること

### カテゴリ判定の処理順序・判定方式（決定事項）
カテゴリ判定（4章「2. 分類」）は、以下の順序・方式で行う（2026-09決定）。

1. **特定受信アドレス宛のメール**：宛先アドレスが、設定画面に登録された「特定受信アドレス一覧」（複数登録可）に一致するかどうかで判定する**ルールベース判定**（詳細設計_特定受信アドレス.md参照）
2. **日程調整・アポイント**：メール内容が、設定画面に登録された判定用キーワード一覧に一致するかどうかで判定する**ルールベース判定**（詳細設計_日程調整アポイント.md参照）
3. **定型的な問い合わせ対応**：上記1・2のいずれにも該当しなかったメールに対し、AIが内容を読んで該当するかを判断する**AI判定**（詳細設計_下書き自動生成.md参照。差出人が既知の連絡先かどうかも含め、外部データとの機械的な照合は行わずAIへの判断材料の1つとして扱うのみとする）
4. 上記1〜3のいずれにも該当しない場合（または3でAIが確信を持って判定できない場合）は「未分類メール」として扱う

なお、営業・取引先とのやり取りは現在保留（上記2.参照）のため、上記の判定フローには含まれない。

ダッシュボードのメール一覧では、各メールの判定方式（ルール判定／AI判定）をバッジで表示する（5章参照）。

### 今回は見送る機能（将来検討）
まずはコア機能（下書き自動生成）でサービスとして立ち上げることを優先し、以下は後回しにする。

- **スレッド要約機能**：他の機能がある程度実現してから検討する
- **添付ファイル忘れ検知機能**：開発ボリューム自体は小さいが、まずはコア機能を優先し後回しにする
- **未返信フォローアップ検知機能**：同上の理由で、まずは見送る

### 対応しない機能（意図的にスコープ外）
- **なりすまし・マルウェア検知機能**：当初検討していたが、以下の理由により本ツールでは実装しない方針とした。
  - 検知精度で、Gmail自体の保護機能や専門のセキュリティ製品（メールセキュリティゲートウェイ、アンチウイルスソフトなど）と戦うのは現実的でない
  - 見逃し（フォールスネガティブ）のリスクが大きく、実害（マルウェア感染・金銭被害）に直結しうる
  - 「注意喚起のみ」に留めても、中途半端な検知は「AIがチェック済み」という誤った安心感を生み、かえって利用者自身の警戒心を下げる恐れがある
  - セキュリティ面の判断は、既存のGmailの保護機能・専用のセキュリティソフトに委ねる方が合理的

## 3. 自動化レベル

運用モードとして、モードA（下書き自動生成）とモードB（全自動送信）を定義するが、**フェーズ1ではモードAのみを実装する**。モードBは今すぐ作り込まず、開発・運用を進める中で「このメールパターンなら全自動送信しても問題ない」と判断できたものから、段階的に対象を追加していく方針とする。

- **モードA：下書き自動生成**（フェーズ1で実装）
  - AIが返信案を作成し、Gmailの下書きとして保存する、または別途確認できる形で提示する
  - 送信は人が内容を確認したうえで実行する
  - 下書きができたことは、後述のダッシュボードで通知・一覧化する（5章参照）
  - 当面はすべてのメールをこのモードで扱う
- **モードB：全自動送信**（将来・段階的に追加）
  - AIが返信案を作成し、確認なしでそのまま送信まで行う
  - フェーズ1では導入しない。モードAでの運用実績を積み、特定のメールパターン（例：日程調整の定型返信など）について「誤送信リスクが十分低い」と確認できた場合に、そのパターンに限定して個別に有効化していく
  - 理由：誤送信リスク、文脈やニュアンスの読み違えリスク、機密情報を含むメールへの誤対応リスクがあるため、実績のないパターンにいきなり適用しない
  - どのパターンをモードBに昇格させるかの判断基準は、開発を進めながら別途整理する（下書きの修正率は計算しない方針としたため判断材料には使わない。7章のOpen Questions参照）

### 対象外とするメール
- **迷惑メール（スパム）フォルダに分類されているメールは、下書き自動生成の対象外とする**
  - フィッシング・詐欺メールが多く含まれる領域のため、AIが返信案を作ること自体を行わない
  - ただし後述の「誤判定検知機能」により、迷惑メール判定が誤っている可能性がある正規メールは別途拾い上げる
- **迷惑メール（スパム）フォルダ以外の受信メールについては、なりすまし・マルウェアの検知は行わない**（2章「対応しない機能」参照。セキュリティ面はGmail自体の保護機能・セキュリティソフトに委ねる）

## 4. 全体ワークフロー（案）

フェーズ1（モードAのみ）の想定フロー：

1. **受信検知**：GAS（Google Apps Script）の時間主導型トリガーにより、Gmailの新着メールを定期的にポーリングして検知する（2026-09-15決定。下書き自動生成・日程調整アポイント・特定受信アドレス・迷惑メール誤判定検知の全カテゴリ共通の方式とする。GAS運用上の注意点は`共通基盤_GAS運用の注意点.md`、迷惑メール誤判定検知機能固有の検証結果は`検証_迷惑メール誤判定検知_実装方式.md`参照）
2. **分類**：メール内容をカテゴリ判定する（定型問い合わせ／営業・取引先／日程調整／特定受信アドレス／その他）
3. **返信案生成**：カテゴリや過去のやり取りの文脈を踏まえ、AIが返信文面を作成する
4. **下書き保存・通知**：Gmailの下書きとして保存し、ダッシュボードに「要確認の下書き」として反映する。あわせてSlackへ通知する（2026-09決定。詳細は5章「Slack通知機能」参照）
5. **確認・送信**：利用者が下書きを確認・編集し、送信する
6. **記録**：対応状況（未対応／下書き済み／送信済みなど）を記録し、ダッシュボードに反映する

モードB（全自動送信）を有効化したパターンについては、上記3の後に確認を挟まず送信まで行い、結果を6の記録に反映する形に拡張する（将来対応）。

## 5. ダッシュボード機能（案）

受信メールの対応状況を一覧化し、ダッシュボードで可視化する。

想定する表示内容（要検討・すり合わせ。カテゴリによって表示項目・対応状況の軸が異なる場合がある。詳細は各カテゴリの詳細設計書を参照）：
- 受信日時、差出人、件名
- カテゴリ（自動分類の結果）
- 対応状況（未対応／保留（回答情報不足）／下書き作成済み／送信済み）
  - 日程調整・アポイントでは、パターンB（相手提示日への自動予定登録）実行時に、これらとは区別できる「予定登録済み・下書き確認待ち」という状態を用いる（詳細設計_日程調整アポイント.md 4章Step5参照）
- 下書き案へのリンク

### 「特定受信アドレス一覧」について
特定受信アドレス宛のメール（2章4.）は、上記の一般的な表示項目・対応状況とは別に、専用の一覧画面「特定受信アドレス一覧」を持つ。受信時に自動でGmail下書きを作成しない点（利用者操作によるワンクリック下書き作成のみ用意）や、表示項目（受信日時、会社名、担当者名、メールアドレス、電話番号、要約、種別、フォーム選択カテゴリ、元メールへのリンク、対応状況）、対応状況の軸（未対応／対応済み／保留の3種類。他カテゴリの「下書き作成済み・未送信」等の軸とは別管理）が他カテゴリと異なる（詳細設計_特定受信アドレス.md 0章・4章Step5参照）。

### 「要確認の下書き」の告知
フェーズ1ではモードA（下書き生成のみ）で運用するため、AIが下書きを作成したメールは必ず人の確認待ちになる。確認漏れ・見逃しを防ぐため、ダッシュボード上で「下書き作成済み・未送信」の状態を目立つ形で告知する（例：件数バッジ、専用の一覧セクションなど）。これにより、下書きが作られたまま放置される事態を防ぐ。

### Slack通知機能（2026-09決定）
ダッシュボードでの確認に加えて、下書き作成時にSlackへ通知する機能を追加する。ダッシュボード自体である程度の確認は完結するため必須の機能ではないが、将来的な他システム連携（Slack等）の実装経験を積む目的も兼ねて導入する。通知タイミング（下書き作成時のみか、保留・注意メッセージ発生時も含めるか）、メッセージ内容、通知先チャンネルの設定方法などの詳細は別途検討する。迷惑メール誤判定検知機能が「正規メールの可能性が高い」と判定した結果も、通知対象に含める（2026-09-16決定。詳細設計_迷惑メール誤判定検知.md 3章Step3参照）。

### 「保留メール」の告知
下書き自動生成機能や日程調整・アポイント機能などが、回答情報の不足・複合質問・候補日の解析不可・候補日0件・パターン判別不明などの理由で下書きを作成せず保留にしたメールを、理由を添えて一覧表示する。下書き自体が作られないため「要確認の下書き」の告知には乗らず、放置されると対応漏れにつながるおそれがある。そのため「要確認の下書き」の告知と同様に、目立つ形で別途表示する（詳細設計_下書き自動生成.md 3章Step3・詳細設計_日程調整アポイント.md 4章Step3・Step4-A・Step4-B・6章参照）。

### カテゴリ固有の注意メッセージ
保留（対応そのものを止めて人の確認を待つ状態）とは別に、処理は継続しつつも利用者に知らせておくべきケースがある（例：日程調整・アポイントで相手提示日に予定を自動登録した場合の通知、提示候補がすべて埋まっていたため代替日を提示した場合の通知、一部候補の日時が解析できなかった場合の通知など）。これらはカテゴリごとに内容が異なるため、「要確認の下書き」「保留メール」の告知とは別枠の注意メッセージとしてダッシュボードに表示する（詳細は各カテゴリの詳細設計書を参照。例：詳細設計_日程調整アポイント.md 4章Step4-A・Step4-B・5章）。

### 手動カテゴリ再分類機能
カテゴリ判定の見落とし（本来該当するカテゴリに振り分けられず、他カテゴリ扱い・未分類のまま残っているメール）に利用者が気づいた場合、ダッシュボード上から該当メールのカテゴリを手動で付け直し、そのカテゴリの下書き生成処理を再実行できるようにする。特定のカテゴリに限らず、下書き自動生成の対象となる全カテゴリで共通して使えるダッシュボード側の機能と位置づける（詳細設計_日程調整アポイント.md 4章Step2「判定漏れメールの手動再分類（ダッシュボード連携）」で必要性が明記されたもの。本機能はダッシュボード機能として今回初めて親仕様に追記するため、具体的な画面仕様は別途まとめる）。

### 「未分類メール」の一覧表示
カテゴリ判定（2章「カテゴリ判定の処理順序・判定方式」参照）でどのカテゴリにも該当しなかったメールを、「未分類」バッジを付けて一覧表示する。全体サマリーの円グラフにある「未分類メールの件数」は集計値のみだが、こちらは個別のメールを確認し、必要に応じて手動カテゴリ再分類機能（上記参照）につなげるための一覧である。

### 判定方式のバッジ表示
ダッシュボードのメール一覧では、各メールのカテゴリ判定がどの方式で行われたかを、バッジで表示する。
- **ルール判定**：特定受信アドレス宛のメール（宛先アドレス一致）、日程調整・アポイント（キーワード一致）
- **AI判定**：定型的な問い合わせ対応（AIによる総合判断）

これにより、利用者は各メールがどのロジックで分類されたかを一目で把握できる（2章「カテゴリ判定の処理順序・判定方式」参照）。

### 全体サマリー（グラフ表示）

**円グラフで表示する項目**
- 全体の受信メール数
- スパムメール数
- 自動返信下書き機能が動いた件数
- 特定受信アドレス宛の受信件数（2026-09改称：旧「ホームページからの問い合わせメール件数」）
- 社外メール受信件数
- 社内メール受信件数
- 迷惑メール誤判定検知機能で見つかった件数（6章参照）
- カテゴリ別受信件数（定型的な問い合わせ対応／営業・取引先とのやり取り／日程調整・アポイント／特定受信アドレス宛、の1〜4それぞれの件数と、いずれにも該当しない「未分類メール」の件数。2章「分類結果の扱い」参照）
- 保留件数（回答情報不足、候補日解析不可・候補日0件・パターン判別不明などの理由により下書きが作成されなかった件数。上記「保留メール」の告知の集計対象）

**その他の表示項目**
- 「要確認の下書き」の滞留日数（下書き作成から確認・送信されるまで何日放置されているか）を、横型の棒グラフで表示する（棒の長さに加え、日数の数値も併記する）

## 6. 迷惑メール誤判定検知機能（案）

Gmailの迷惑メール判定で、本来は正規のメールが誤って迷惑メールフォルダに振り分けられ、見逃されるケースがある。これを防ぐための検知機能。

> **【検証結果・実装方式決定】** Claude用Gmailコネクタで検証したところ、`in:spam` / `is:spam` / `label:SPAM` などいずれのクエリでも検索結果が0件だった。`list_labels`ではSPAMラベルにメールが存在することが確認できるため、データはあるが検索経由ではアクセスできない状態。ツールの仕様上も、サポートされる`in:`演算子に`spam`が含まれておらず、コネクタ側で意図的にアクセスを制限している可能性が高い（詳細は`検証_迷惑メール誤判定検知_実装方式.md`参照）。
> 本機能はGoogle Apps Script（GmailApp.searchなど）を使って実装する方針を正式決定した（2026-09決定）。詳細仕様（機能仕様レベル）を別紙にまとめた：`詳細設計_迷惑メール誤判定検知.md`

### 目的
迷惑メールフォルダの中身を定期的にAIが確認し、「実際は正規のメールらしいもの」を見つけて知らせる。自動返信・自動対応は行わない（本セクションのスコープは「見逃し防止の通知」まで）。

### 想定ワークフロー
1. **定期スキャン**：迷惑メールフォルダ内のメールを一定間隔で確認する。スキャン頻度は、ダッシュボードから設定画面を開いて利用者が設定できるようにする
2. **誤判定らしさの判定**：AIが以下のような観点で「正規メールの可能性が高いか」を判断する
   - 差出人が既知の取引先・過去にやり取りのあるアドレスかどうか
   - 本文がフィッシング・詐欺特有の特徴（不自然なリンク、緊急性を煽る文言、金銭・個人情報の要求など）を含まないか
   - 件名・本文の内容が業務上あり得る自然な文脈かどうか
3. **通知**：正規メールの可能性が高いと判定したものを一覧化し、利用者に通知する（ダッシュボードでの一覧表示、または別途通知）
4. **対応**：迷惑メールでないという判断は利用者が行い、受信トレイに戻す

### 注意点
- 誤判定検知の精度も完璧ではないため、判定基準は「怪しいけど一応知らせる」くらいの緩めに設定する方針とした（2026-09決定。正規メールが迷惑メール判定されること自体がそれほど多くない想定のため、基準を緩めにしてもノイズになるほどの通知件数にはならないと判断。7章「決定済み事項」参照）

## 7. 検討が必要な論点（Open Questions）

- **モードBへの昇格基準**：モードAでの運用実績をどう蓄積・評価し、「このパターンは全自動送信してよい」とどう判断するか（例：一定件数・期間の実績、誤送信や重大なトラブルが発生していないことなど、判断材料の指標化が必要）
- **文脈・文体の参照**：AIが返信文面を作る際に、相手や関係性に応じて文体（丁寧さ・言い回し）をどこまで調整するかという論点。例えば、何度もやり取りしている親しい相手には少しくだけた言い回しにし、初めての相手や形式的な相手には丁寧な言い回しにする、といった使い分けを行うかどうか。また、「利用者の文体プロファイル活用」とは、うねのさん自身が過去に送ったメール（Gmail送信履歴）を学習材料にして、AIが生成する下書きの言い回し・語彙・署名の付け方などを、うねのさんらしい文体に近づける、という考え方を指す。現時点ではどこまで作り込むか（相手ごとの調整はしないでまず一律の丁寧な文体にするのか、文体プロファイルの学習まで行うのか）が決まっていない。
- **データの取り扱い**：本ツールが扱うメール本文や、特定受信アドレス機能で抽出する会社名・電話番号などの個人情報を、Gmail以外の場所（本ツール側のデータベースやログなど）にどこまで保存するか、という論点。前提として、AIによる分類・下書き生成の都度、メール本文はAnthropicのAPIに送信される（外部にデータを渡している）。それとは別に、動作記録・エラー記録・AIとのやり取りの履歴などをデバッグ目的で保存する場合、その保存先・保存期間・アクセス権限をどうするかが未定。個人利用の間は厳密さの優先度は低いが、将来企業へ販売する際には、顧客企業の営業秘密や取引先の個人情報を扱うことになるため、プライバシーポリシー・契約レベルの取り決めが必要になる。
- **API利用コストの実測・見積もり**：メール1件あたりの実際のトークン消費量（本文の長さ、スレッド履歴の有無などで変動）を計測し、Haiku/Sonnetどちらのモデルが適切か、月額コストの見積もり精度を高める。参考試算（入力2,000トークン・出力500トークン/件と仮定）：Claude Haiku 4.5で約0.7円/件、Claude Sonnet 4.6で約2円/件。プロンプトキャッシングやバッチ処理を使えばさらに削減できる見込み

### 決定済み事項（参考）
以下は検討の結果、本ドキュメント内で決定済みとした項目（旧Open Questions）。
- **分類の精度**：カテゴリ判定は「特定受信アドレス宛のメール（ルールベース・宛先一致）→日程調整・アポイント（ルールベース・キーワード一致）→定型的な問い合わせ対応（AI判定）」の順に行う方針とした（2026-09決定）。いずれにも該当しない場合は「未分類メール」として扱い、ダッシュボードに「未分類」バッジで一覧表示する（判別不能等の別ステータスは設けない）。詳細は2章「カテゴリ判定の処理順序・判定方式」・5章参照。
- **下書き修正率の判定基準**：計算・ゲージ表示は行わない方針とした（2026-09決定。仕組みが複雑になる割に必要性が低いと判断）。5章のゲージ表示項目は削除済み。モードBへの昇格基準の判断材料からも外した（7章参照）。
- **除外ルール**：フェーズ1では、クレーム・法務が絡む案件・機密性の高い内容などを特別に検知して除外する仕組みは設けない方針とした（2026-09決定）。個人利用でメール処理件数自体が少ない想定のため。将来、処理件数が増えて見逃しリスクが顕在化した場合に改めて検討する。
- **通知方法**：ダッシュボードでの告知に加えて、Slackへの通知機能を追加する方針とした（2026-09決定。詳細は5章「Slack通知機能」参照）。
- **ダッシュボードの利用者・更新頻度**：フェーズ1では利用者はうねのさん本人のみとし、チーム共有は行わない方針とした（2026-09決定）。企業向け展開時にはチーム共有・権限管理の機能が必要になる（8章「マルチテナント・権限管理」参照）。リアルタイム性については、メール処理（受信検知→分類→下書き生成）が完了した時点でダッシュボード側のデータを更新し、画面は短い間隔でのポーリングまたはプッシュ更新により、体感的にほぼ即時反映されることを目指す方針とした（個人利用規模のメール量であれば技術的に十分実現可能と判断）。
- **誤判定検知の閾値**：迷惑メール誤判定検知機能（6章）の判定基準は、緩め（怪しければ一応知らせる）に設定する方針とした（2026-09決定。詳細は6章「注意点」参照）。
- **新着メール検知の連携方式**：下書き自動生成・日程調整アポイント・特定受信アドレス・迷惑メール誤判定検知の全カテゴリで、GAS（Google Apps Script）の時間主導型トリガーによるポーリング方式に統一する方針とした（2026-09-15決定。4章参照）。理由：（1）迷惑メール誤判定検知機能はGmailコネクタでスパムフォルダにアクセスできないため、もともとGAS採用が必須だった、（2）他の3機能もメール到着から数分〜十数分程度の遅れは実用上問題にならない想定のため、GASのポーリングで十分間に合う、（3）4機能をGASに統一することで、後述の認可エラー対応・監視の仕組みを共通化でき、実装・保守の効率もよい、（4）うねのさん自身にGASでの開発実績（Ysystems向け求人管理システム）があり、技術的にも親和性が高い。
- **Gmail連携の認可エラー対応方針**：GASでGmailと連携する機能は、Gmail連携の認可（OAuth）が何らかの理由で失効すると、トリガーが停止し**自動では復旧しない**という特性を持つ（人手による再認可操作が必須。詳細は`共通基盤_GAS運用の注意点.md`参照）。このため、下書き自動生成・日程調整アポイント・特定受信アドレス・迷惑メール誤判定検知の全カテゴリで、Gmail連携（日程調整アポイントはGoogleカレンダー連携を含む）の認可エラー（GASでは「Authorization is required to perform that action」等の特定メッセージを含む例外として発生する）を検知した場合は、通常の処理エラーと区別し、ダッシュボードの処理エラーアラート一覧（詳細設計_ダッシュボード.md 9章）に「要再認証」種別として記録する方針とした（2026-09決定）。各カテゴリの詳細設計の例外処理章にも、この検知ロジックを個別に明記する。なお、この種の「連携が時々切れて再認可が必要になる」こと自体はGAS固有の欠陥ではなく、Gmail連携全般に共通する業界共通の課題である（`共通基盤_GAS運用の注意点.md` 2(c)参照）。
- **導入効果の見立て**：Open Questionとして明示的に管理する必要はないと判断し、削除した（2026-09決定）。
- **受信メールのグループ化・フォルダ分け機能／グループ化ルールの管理**：機能自体を完全削除した（2026-09決定）。既に「定型的な問い合わせ対応」「日程調整・アポイント」「特定受信アドレス宛」の3カテゴリ＋「未分類メール」一覧でダッシュボード表示が整理できているため、それ以上の分類機能は不要と判断。5章の該当セクションおよび全体サマリーの「転職エージェント・フリーランス案件紹介サイトからのメール件数」の項目も削除済み。

## 8. 将来展望（企業向け展開を見据えて）

現時点では個人用として作るが、将来の企業向け展開を見据えて、以下のような点は頭の片隅に置いておく（今のフェーズで作り込む必要はない）。

- **個人設定と業務ロジックの分離**：文体・署名・除外ルールなど「利用者個人の設定」と、「カテゴリ分類・下書き生成などの汎用ロジック」を分けておくと、他者・他社向けに展開しやすい
- **マルチテナント・権限管理**：企業向けにする場合、複数ユーザー・複数会社のデータを分離し、アクセス権限を制御する仕組みが必要になる（今は不要だが設計時に意識）
- **ノウハウの記録**：うまくいった工夫、失敗した試み、精度が出なかったケースなどを、開発と並行してメモに残しておく（キャリア資産・将来の提案資料の元ネタとして）
- **企業向け販売時のAI接続・収益モデル（BYOK前提）**：企業向けに販売する場合、本ツールはClaude（Anthropic API）をサーバー側から呼び出す構成に作り変える必要がある。ライセンス形態は「BYOK（Bring Your Own Key）」を基本とする：顧客企業自身にAnthropicのアカウントを作成・契約してもらい、その顧客名義のAPIキーを本ツールに登録して動かす。うねのさん自身の名義・アカウントで複数顧客分をまとめて動かす方式は、Anthropicの利用規約（単一のサブスクリプション・APIキーで複数エンドユーザーを認証することの制限）に抵触しうるため避ける。
  収益は3段階に分ける想定：
  - （1）初期のオンボーディング費用：Anthropicアカウント登録・APIキー発行の代行サポート（買い切り）
  - （2）ソフトウェアの月額利用料：うねのさんへ（本ツール自体の対価）
  - （3）API使用量に応じた従量課金：Anthropicへ、顧客が直接支払う（うねのさんはAPIコスト変動リスクを負わない）

  **顧客企業側のアカウント構成について**：Anthropicの組織アカウント（Organization）は1つの中に複数の「ワークスペース」・APIキーを作成でき、請求は組織単位で一本化される。そのため、社員数分（例：100人なら100個）のアカウントを作る必要はなく、顧客企業として1つのアカウントを契約すれば、その中で部署・環境ごとにAPIキーを分けて社員全員分の利用をまかなえる。なお、座席（シート）課金型の「Claude Enterprise」のようなプランも別途存在するが、これは人がChat形式で直接使う用途向けであり、本ツールのようにサーバー側からAPIを呼び出すバックエンド利用では基本的に不要（素のAPI従量課金のみで足りる）。

- **競合調査：類似ツールにおけるAI機能の価格設定傾向**（2026-09調査）：国内の類似ツール（楽楽自動応対〔旧メールディーラー〕・株式会社ラクス、yaritori・Onebox株式会社、AIto・メディアリンク株式会社）を調査した結果、いずれも基本料金（メール共有・問い合わせ管理などのベース機能分）とは別に、AI機能（返信文の自動生成、クレーム自動検知など）を個別オプション・個別見積もり扱いとしており、具体的な金額を公開していないケースが大半だった。**「AI機能部分は個別見積もりが業界の通例」**と言える。唯一yaritoriのみベース料金（初期費用0円・月額1,980円〜/ユーザー）を公式サイトで公開しているが、AI機能（yaritori AI Pro等）自体の追加料金は非公開だった。本ツールの将来の価格設計（上記（2）ソフトウェアの月額利用料）を検討する際、AI機能をベース料金に含めるか、追加オプション課金にするかの参考情報とする。

- **価格方針（案）：1アカウント月額500円程度**（2026-09判断）：上記の競合調査を踏まえ、（2）ソフトウェアの月額利用料は1アカウントあたり月額500円程度の低価格に設定する方針とした。価格を抑えることで導入のハードルを下げ、まずは導入社数・導入アカウント数を稼ぐことを優先する（薄利多売型の戦略）。なお、この金額は現時点での方向性であり、実際の開発・運用コストや、（1）オンボーディング費用・（3）API従量課金（顧客が別途Anthropicへ直接支払う分）とのバランスを見ながら、今後見直す可能性がある。

## 9. 次のステップ（案）

1. Gmail連携の技術的な実現方法を確認する（Claude用のGmailコネクタなど）
2. 上記4カテゴリのうち、最初に試すカテゴリを1〜2個に絞り込む
3. そのカテゴリに絞って、分類ロジックと下書き生成プロンプトのたたき台を作る
4. 小規模に試験運用し、下書きの質・工数削減効果を検証する
5. 並行してダッシュボードの表示項目・画面イメージを固める
