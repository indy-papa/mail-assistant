# 詳細仕様：迷惑メール誤判定検知機能

対象カテゴリ：迷惑メール誤判定検知
親仕様：メールアシスタントツール_仕様書.md 6章「迷惑メール誤判定検知機能（案）」
関連：検証_迷惑メール誤判定検知_実装方式.md（技術検証・実装方式の調査結果）／共通基盤_GAS運用の注意点.md（GAS共通の運用上の注意点）／詳細設計_ダッシュボード.md 8章・9章・11章・13章

作成日: 2026-09-15

## 0. 本カテゴリの位置づけ（他カテゴリとの違い）

他の3カテゴリ（下書き自動生成・日程調整アポイント・特定受信アドレス）は、受信トレイの新着メールを対象に、返信の下書き作成や情報抽出を行う機能。本機能は対象がまったく異なり、**迷惑メールフォルダ（スパム）**の中身を定期的にチェックし、「本来は正規のメールなのに誤って迷惑メール判定されてしまったもの」を見つけて利用者に知らせる機能である。返信の下書き作成・自動対応は一切行わない（見逃し防止の通知のみ。親仕様6章参照）。

また、本機能はGmailコネクタでは迷惑メールフォルダにアクセスできないことが判明したため、GAS（Google Apps Script）による実装が前提となる（検証_迷惑メール誤判定検知_実装方式.md参照。なお現在は他の3カテゴリもGASに統一する方針が決定済みのため、この点は全カテゴリ共通。メールアシスタントツール_仕様書.md 4章参照）。

## 1. トリガー

GAS（Google Apps Script）の時間主導型トリガーにより、迷惑メールフォルダの中身を定期的にスキャンする（2026-09-15決定。全カテゴリ共通の連携方式。親仕様4章参照）。スキャン頻度は、ダッシュボードの設定画面から利用者が変更できるようにする（親仕様6章、詳細設計_ダッシュボード.md 11章）。未設定時のデフォルトのスキャン頻度は5分とする（2026-09-16決定。下書き自動生成・日程調整アポイント・特定受信アドレスも含む4機能共通のデフォルト値。共通基盤_GAS運用の注意点.md参照）。

他カテゴリ（新着メール検知）とは異なり、本機能は「フォルダ内の一覧を都度取得して差分を見る」方式になる。`GmailApp.search('in:spam ...')`でメール一覧を取得し、前回チェック時刻以降の新着分のみを対象とする（検証_迷惑メール誤判定検知_実装方式.md 3.1・3.2参照）。

## 2. 入力

- 迷惑メールフォルダ内のメール一覧（`GmailApp.search('in:spam newer_than:...')`等で取得。検証_迷惑メール誤判定検知_実装方式.md 3.1参照）
- 各メールの本文（プレーンテキスト）、件名、差出人アドレス、受信日時
- 前回チェック時刻（`PropertiesService`で保持。差分検知のため。検証_迷惑メール誤判定検知_実装方式.md 3.2・5章参照）
- 差出人が既知の取引先・過去にやり取りのあるアドレスかどうかの判定材料（厳密な外部データ照合は行わず、AIへの判断材料の1つとして扱う。下書き自動生成の方針〔詳細設計_下書き自動生成.md 3章Step2〕と同様の考え方）

## 3. 処理フロー

### Step 1：定期スキャン

1. 前回チェック時刻（`PropertiesService`）以降に迷惑メールフォルダに追加されたメールを取得する（差分方式。検証_迷惑メール誤判定検知_実装方式.md 3.2参照）
2. 導入初回のみ、既存の迷惑メール（うねのさんの環境で現在221件確認済み）を対象に一括チェックを行う。GASの実行時間上限（6分/回）に接近しうるため、初回のみバッチを分割して実行する（例：100件ずつ複数回のトリガー実行に分ける。検証_迷惑メール誤判定検知_実装方式.md 3.2参照）
3. 「最終実行日時」を`PropertiesService`に記録する（共通基盤_GAS運用の注意点.md 5章の監視要件）。**この記録は、処理が正常終了した場合だけでなく、Step2・Step3でエラーが発生してtry/catchで捕捉した場合も行う**（トリガー自体は起動できた＝スクリプトは動いている、という事実を残すため。5章「例外処理」参照）

### Step 2：誤判定らしさの判定（AI判定）

取得した各メールについて、AIが以下の観点をもとに「正規メールの可能性が高いか」を判断する（親仕様6章）。

- 差出人が既知の取引先・過去にやり取りのあるアドレスかどうか
- 本文がフィッシング・詐欺特有の特徴（不自然なリンク、緊急性を煽る文言、金銭・個人情報の要求など）を含まないか
- 件名・本文の内容が業務上あり得る自然な文脈かどうか

判定基準は「怪しいけど一応知らせる」くらいの緩めに設定する方針とした（2026-09決定。親仕様6章「注意点」参照。正規メールが誤って迷惑メール判定されること自体がそれほど多くない想定のため、基準を緩めにしてもノイズになるほどの通知件数にはならないと判断）。したがって、他カテゴリのような確信度としきい値による絞り込みは行わず、AIが「正規メールの可能性がある」と判断したものはすべて通知対象とする。

メール分類（メルマガ系／広告系／支払系／有効期限系／その他）もあわせてAIに判定させる（ダッシュボードの結果一覧の表示項目。詳細設計_ダッシュボード.md 8章参照）。

### Step 3：結果の記録・通知

- 「正規メールの可能性が高い」と判定したメールを、ダッシュボードの「迷惑メール誤判定検知 結果一覧」に追加する（詳細設計_ダッシュボード.md 8章）
- 表示項目：送信元メールアドレス、送信元企業名および担当者名、メール件名、メール分類（詳細設計_ダッシュボード.md 8章参照）
- Slack通知（親仕様5章「Slack通知機能」）の対象に含める（2026-09-16決定）

### Step 4：利用者による対応（ダッシュボード操作）

- 迷惑メールでないという最終判断は利用者が行う。**UIは、ダッシュボードの「迷惑メール誤判定検知 結果一覧」（詳細設計_ダッシュボード.md 8章）に表示されている該当メールの行から、受信トレイへ移動する操作を行う形とする（2026-09-16決定）**
- 実装方法（GmailApp側でどのAPIを使ってラベル・フォルダを操作するか）は、現時点では具体的な選択肢を検討できていないため別途調査のうえ決定する（8章Open Questions参照）

## 4. 出力

- ダッシュボードの「迷惑メール誤判定検知 結果一覧」への新規レコード追加（送信元メールアドレス、送信元企業名・担当者名、件名、メール分類。詳細設計_ダッシュボード.md 8章参照）
- 全体サマリーの「迷惑メール誤判定検知機能で見つかった件数」への集計（親仕様5章）
- 処理エラーアラート一覧への記録（正常終了／エラー。6章参照）
- 「最終実行日時」の更新（`PropertiesService`。監視用）

## 5. 例外処理

- **AIによる判定処理自体がエラーになった場合**：エラーとして記録し、処理エラーアラート一覧（詳細設計_ダッシュボード.md 9章）に記録する。該当メールは**次回スキャンで自動的に再対象とする**（差分検知の基準となる「前回チェック時刻」を、エラーになったメールを処理し切れていない場合は更新しない、または該当メール個別に未処理フラグを残す形にする。実装の詳細は別途）。あわせて、**利用者がダッシュボード上から該当メールを個別に再試行できる操作も用意する**（2026-09-16決定。ボタンの具体的な画面配置は8章Open Questions参照）
- **Gmail連携の認可（OAuth）が失効している場合**：GASでは「Authorization is required to perform that action」等の特定メッセージを含む例外が発生するため、try/catchで検知し、通常の処理エラーとは区別して処理エラーアラート一覧に「要再認証」種別で記録する（2026-09決定）。この種のエラーは放置しても自然には復旧せず、人手による再認可操作が必要となるため区別する。全カテゴリ共通の方針（詳細は`共通基盤_GAS運用の注意点.md`、`メールアシスタントツール_仕様書.md` 7章「Gmail連携の認可エラー対応方針」参照）。**この記録により、詳細設計_ダッシュボード.md 1章「共通ヘッダー：OAuth連携状態バッジ」も赤色に変わる**（別々の仕組みではなく、同じ検知結果を使う）
- **想定スキャン間隔を超えて「最終実行日時」が更新されない場合**：トリガー自体が停止している可能性が高い（共通基盤_GAS運用の注意点.md 2(b)・5章参照。GASのトリガーが原因不明で停止し自然復旧しない不具合への対策）。**これは上記のOAuth認可エラー（try/catchで捕捉できるエラー）とは別の状況を指す**：OAuthエラーはスクリプトが実行された上でエラーになるケース（Step1で述べた通り、この場合も「最終実行日時」は更新される）であるのに対し、本項目はトリガー自体がまったく起動しなくなる（スクリプトが1行も実行されない）ケースを想定しており、OAuthの問題に限らずGoogle側の不具合等でも起こりうる。**判定方法：ダッシュボードを開いた時点で、現在時刻と最終実行日時を比較し、想定スキャン間隔（デフォルト5分）を超えていれば、ダッシュボード上で最終実行日時の文字色を赤く表示する（2026-09-16決定。詳細設計_ダッシュボード.md 9章参照）**。なお、ダッシュボード自体を何で作るか（GASでWebページとして作るのか、他の方法にするのか）がまだ決まっておらず、この土台が決まらないと、上記の比較の計算をサーバー側のコードで行うのかブラウザ側のJavaScriptで行うのかが決められない。そのため、この計算処理を具体的にどこに書くかは、ダッシュボードの実装方式を決める際にあわせて確定させる（8章Open Questions・共通基盤_GAS運用の注意点.md 8章参照）
- **同一メールが複数回の差分スキャンで重複して結果一覧に追加されそうになった場合**：前回チェック時刻以降のメールのみを対象とする差分方式のため、通常は重複は発生しない想定。GASのトリガー実行が重なる（多重起動）リスクについては検討の結果、個人利用の処理量では発生しにくいため、特別な多重起動防止の仕組み（LockServiceなど）は設けない方針とした（2026-09-16決定）

## 6. 他機能との関係

- ダッシュボード（親仕様5章・詳細設計_ダッシュボード.md）：「迷惑メール誤判定検知 結果一覧」（8章）の一次データソース、全体サマリーの集計元、処理エラーアラート一覧（9章）への記録、システム設定画面（11章）でのスキャン頻度設定
- 下書き自動生成・日程調整アポイント・特定受信アドレス：これら3カテゴリのStep 1「除外判定」で、迷惑メールフォルダに分類されているメールは処理対象から除外され、本機能側の警告・通知フローに委ねられる（各詳細設計の該当章参照）
- 共通基盤_GAS運用の注意点.md：GASのクオータ・安定性・認可エラー対応・監視の仕組みは、本機能を含むGAS採用の全カテゴリに共通する内容としてそちらにまとめている

## 7. コスト

- GAS自体の利用は無料
- Claude API呼び出しコストは、Haiku 4.5で約0.7円/件、Sonnet 4.6で約2円/件（入力2,000トークン・出力500トークン/件と仮定。仕様書7章の試算を流用）。スパムメールは本文が短く定型的なものが多いと想定されるため、実際のトークン消費は上記試算より少なくなる可能性がある（実測はフェーズ1の運用を通じて行う。下記参照）
- **完全に無料での実現（AI判定をやめてルールベースにする等）は検討したが、採用しない方針とした（2026-09-16決定）**。個人利用規模の想定件数であればAI判定のコストは月数百円程度に収まり、実質的に無視できる水準であること、ルールベース化すると判定精度が下がり本機能の目的（誤判定の見逃し防止）が損なわれることから、現状のAI判定・低コストの方針のまま進める
- スパムメール1件あたりの実際のトークン消費量・コストの実測は、フェーズ1の運用を通じて行う（2026-09-16決定）。将来、次のフェーズ（企業向け販売等）を検討する段階になった際、フェーズ1で収集したこの実測データをもとにコストを計測・算出する

## 8. このカテゴリ固有のOpen Questions

- 迷惑メールフォルダから受信トレイへ戻す操作の実装方法（GmailApp側でどのAPIを使うか。UI自体は決定済み。3章Step4・詳細設計_ダッシュボード.md 13章と連動）
- 「最終実行日時が想定間隔を超えている」ことの比較処理を、具体的にどこで実行するか。**ダッシュボード自体を何で作るか（GASでWebページとして作るのか、他の方法にするのか）がまだ決まっていない。この土台が決まらないと、比較の計算をサーバー側のコードで行うのかブラウザ側のJavaScriptで行うのかが決められない。**（判定タイミング・表示ルール自体は決定済み。5章参照。共通基盤_GAS運用の注意点.md 8章にも同様の記載あり）

### 決定済み事項（参考）
以下は検討の結果、本ドキュメント内で決定済みとした項目。
- **実装方式**：GAS（Google Apps Script）で実装する（2026-09決定。検証_迷惑メール誤判定検知_実装方式.md参照。Gmailコネクタでは迷惑メールフォルダにアクセスできないため）
- **誤判定検知の閾値**：確信度によるしきい値判定は設けず、AIが「正規メールの可能性がある」と判断したものはすべて通知する、緩めの基準とする（親仕様6章「注意点」参照）
- **差分検知方式**：前回チェック時刻を`PropertiesService`で保持し、新着分のみを対象にする（検証_迷惑メール誤判定検知_実装方式.md 5章参照）
- **導入初回の一括処理**：既存の迷惑メール（221件）は初回のみバッチを分割して処理する（検証_迷惑メール誤判定検知_実装方式.md 3.2参照）
- **認可エラーの検知・区別**：GASの認可エラーをtry/catchで検知し「要再認証」として処理エラーアラート一覧に記録する（共通基盤_GAS運用の注意点.md参照）
- **受信トレイへ戻す操作のUI**：ダッシュボードの結果一覧の行から操作する（2026-09-16決定。3章Step4参照）
- **AI判定エラー時の再試行方針**：次回スキャンでの自動再対象化に加え、ダッシュボードからの個別リトライ操作を用意する。ボタンは共通ヘッダーのOAuthバッジの近くに設置する（2026-09-16決定。5章・詳細設計_ダッシュボード.md 1章参照）
- **GASトリガーの多重起動対策**：個人利用の処理量では発生しにくいリスクと判断し、LockServiceなどの特別な対策は設けない（2026-09-16決定。5章参照）
- **Slack通知の対象**：本機能の結果もSlack通知の対象に含める（2026-09-16決定。3章Step3参照）
- **コスト方針**：AI判定・低コスト（月数百円程度の試算）の現状方針のまま進める。完全無料化のためのルールベース化は精度低下を理由に不採用（2026-09-16決定。7章参照）
- **トークン消費量の実測方針**：フェーズ1の運用を通じて実測し、将来の次フェーズ検討時にそのデータをもとに算出する（2026-09-16決定。7章参照）
- **スキャン頻度のデフォルト値**：5分とする（2026-09-16決定。下書き自動生成・日程調整アポイント・特定受信アドレスも含む4機能共通のデフォルト値。1章・共通基盤_GAS運用の注意点.md参照）
- **「最終実行日時」の異常判定タイミング・表示ルール**：ダッシュボードを開いた時点で、想定スキャン間隔を超えて更新がなければ最終実行日時の文字色を赤くする（2026-09-16決定。5章・詳細設計_ダッシュボード.md 9章参照）
