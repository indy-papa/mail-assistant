# 詳細仕様：ダッシュボード機能

親仕様：メールアシスタントツール_仕様書.md（v0.17）5章
関連詳細設計：詳細設計_下書き自動生成.md／詳細設計_日程調整アポイント.md／詳細設計_特定受信アドレス.md／詳細設計_迷惑メール誤判定検知.md

作成日: 2026-09-03

## 0. 本機能の位置づけ

他の4つの詳細設計（下書き自動生成・日程調整アポイント・特定受信アドレス・迷惑メール誤判定検知）は、いずれもGAS（Google Apps Script）のトリガーで定期的に動く処理機能だが、ダッシュボード機能はメール受信をトリガーとせず、各処理機能が生成・更新するデータ（下書きステータス、保留理由、注意メッセージ、特定受信アドレスレコード、誤判定候補、エラーログなど）を集約して可視化・操作する画面である。そのため本ドキュメントの構成は、他の4つの詳細設計（トリガー→入力→処理フロー→出力の順）とは異なり、画面構成ごとに仕様を記述する形をとる。

## 1. 画面構成（メニュー構成案）

ダッシュボードは以下のセクション（画面／タブ）で構成する想定。

1. 全体サマリー（トップ画面。2章）
2. 要確認の下書き一覧（3章）
3. 保留メール一覧（4章）
4. カテゴリ固有の注意メッセージ（5章）
5. 特定受信アドレス一覧（7章）
6. 迷惑メール誤判定検知 結果一覧（8章）
7. 処理エラーアラート一覧（9章）
8. （廃止）受信メールのグループ化・フォルダ表示：親仕様7章の決定により機能自体を完全削除したため、本画面は設けない（10章参照）
9. システム設定画面（バッジから呼び出し。11章）

なお「手動カテゴリ再分類機能」（6章）は専用の画面を持たず、上記の各メール一覧（要確認の下書き一覧・保留メール一覧など）の行に組み込む操作として提供する。

各セクション間の画面遷移・ナビゲーションの具体的なUIレイアウトは、ワイヤーフレーム作成時に別途定める（13章Open Questions参照）。

### 共通ヘッダー：OAuth連携状態バッジ（2026-09-15追加）

上記のどの画面を表示していても常に見える位置（画面右上）に、GAS↔Gmail（日程調整・アポイントについてはGoogleカレンダーも含む）連携の認可（OAuth）状態を示すバッジを常設する。特定の画面に属する表示ではなく、全画面共通のヘッダー要素とする。

- **通常時（認可が有効な場合）**：濃いグレーで「OAuth」と表示する
- **いずれかのカテゴリで認可エラー（要再認証）を検知した場合**：同じバッジが赤色に変わる（表示テキストは「OAuth」のまま、色の変化だけで異常を知らせる）
- 色を変えるトリガーは、各カテゴリの例外処理でtry/catchにより検知している認可エラー（共通基盤_GAS運用の注意点.md 3章、処理エラーアラート一覧の「要再認証」種別。9章参照）と同じ検知結果を使う。4カテゴリ（下書き自動生成・日程調整アポイント・特定受信アドレス・迷惑メール誤判定検知）はいずれもGASでGmail（またはGoogleカレンダー）と連携しているため、本バッジは特定のカテゴリに紐づくものではなく、システム全体としてのOAuth連携状態をまとめて表す
- バッジをクリックすると、再認可手順マニュアル（9章「エラーの場合のエラー内容」参照）へ直接遷移する（2026-09-17決定）
- **OAuthバッジのすぐ近くに、AI判定エラー時の「個別リトライ」操作の入り口（ボタン等）を設置する（2026-09-16決定）**。クリックすると、AI判定処理がエラーになった項目（処理エラーアラート一覧〔9章〕に記録されているもの）に対して再試行を行う。具体的なUI（ボタン単体か、クリックで一覧を開く形か等）はワイヤーフレーム作成時に別途定める（13章Open Questions参照）

## 2. 全体サマリー

親仕様5章の内容をそのまま踏襲する。

**円グラフ**
- 全体の受信メール数
- スパムメール数
- 自動返信下書き機能が動いた件数
- 特定受信アドレス宛の受信件数
- 迷惑メール誤判定検知機能で見つかった件数
- 転職エージェント・フリーランス案件紹介サイトからのメール件数（グループ化機能）
- カテゴリ別受信件数（定型的な問い合わせ対応／営業・取引先／日程調整・アポイント／特定受信アドレス宛／迷惑メール誤判定検知、および未分類メール。社外／社内の区分は廃止した。2026-09-17決定）
- 保留件数

各円弧グラフ（カテゴリ別受信件数）はクリック可能で、対応する一覧画面（要確認の下書き一覧〔3章〕、特定受信アドレス一覧〔7章〕、迷惑メール誤判定検知結果一覧〔8章〕等）に遷移する（2026-09-17決定）。

**その他**
- 「要確認の下書き」の滞留日数（横棒グラフ、日数を併記）

データソース：各カテゴリの詳細設計「出力」章に記載された集計用メタ情報（詳細設計_下書き自動生成.md 4章、詳細設計_日程調整アポイント.md 5章、詳細設計_特定受信アドレス.md 5章）。

## 3. 要確認の下書き一覧

対象：AIが下書きを作成し、まだ送信されていないメール（親仕様5章「要確認の下書きの告知」）。

本画面へは、全体サマリー（2章）のカテゴリ別受信件数の円弧グラフのうち「下書き」の弧をクリックすることで遷移する（2026-09-17決定）。

表示項目：
- 受信日時、差出人、件名
- カテゴリ
- 下書きへのリンク（クリックでGmailの下書き画面を開く）
- 「不要」ボタン／バッジ
- 下書き本文に不確定マーカー（`<<<<〇〇について回答不確定>>>>`形式）が含まれる場合の要確認フラグ表示（詳細設計_下書き自動生成.md 3章Step3・5章参照。2026-09追加）。定型的な問い合わせ対応では回答情報が不足していても保留にせず必ず下書きを作成する方式（2026-09決定）としたため、このフラグにより「送信前に必ず内容確認・加筆が必要な下書き」を他の下書きと区別できるようにする

操作・仕様：
- フェーズ1では、ダッシュボード上での下書き本文の編集機能は設けない
- 返信不要なメールに対しても自動で下書きが作成されてしまうケースを想定し、「不要」を押すとその下書きを本一覧から削除できるようにする。削除時はGmail側の下書き自体も削除する（ダッシュボードの管理データのみを消して表示から外す方式は採らない。2026-09-17決定）
- 日程調整・アポイントでパターンBにより予定が自動登録された下書きは、他カテゴリと区別できるステータス表示（例：「予定登録済み・下書き確認待ち」バッジ、詳細設計_日程調整アポイント.md 4章Step5）を行う

## 4. 保留メール一覧

対象：日程調整アポイントなどが、候補日解析不可・候補日0件・パターン判別不明などの理由で下書きを作成せず保留にしたメール（詳細設計_日程調整アポイント.md 4章Step3・Step4-A・Step4-B、6章）。

下書き自動生成（定型的な問い合わせ対応）は2026-09の方針変更により「保留」の運用を廃止し、回答情報が不足する場合も含めて必ず下書きを作成する方式としたため、本一覧の対象外とする（詳細設計_下書き自動生成.md 3章Step3・5章参照）。該当する下書きは、3章「要確認の下書き一覧」に不確定マーカー付きの要確認フラグとして表示される。

表示項目：
- 受信日時、差出人、件名
- カテゴリ
- 保留理由（例：「日程調整・候補日なし」「日程調整・日時解析不可」「日程調整・判定不明」）
- 元メールへのリンク

## 5. カテゴリ固有の注意メッセージ

対象：保留ではないが利用者に知らせるべきケース（例：日程調整・アポイントでの自動予定登録通知、代替日提示の通知、一部候補の日時解析不可の通知。詳細設計_日程調整アポイント.md 4章Step4-A・Step4-B）。

表示項目：
- 発生日時
- カテゴリ
- メッセージ内容
- 元メール・関連下書きへのリンク

「要確認の下書き」「保留メール」の告知とは別枠のセクションとして表示する（親仕様5章）。

## 6. 手動カテゴリ再分類機能

対象：カテゴリ判定の見落とし（本来該当するカテゴリに振り分けられず、他カテゴリ扱い・未分類のまま残っているメール）に利用者が気づいた場合の手動修正機能（親仕様5章、詳細設計_日程調整アポイント.md 4章Step2で必要性が明記）。

画面：専用の一覧画面は持たず、メール単位の各一覧（要確認の下書き一覧・保留メール一覧・受信トレイ相当の一覧など）の各行から呼び出せる操作として提供する。

操作フロー：
1. 利用者が対象メールの行で「カテゴリ変更」を選択する
2. 変更後のカテゴリ（定型的な問い合わせ対応／日程調整・アポイント／特定受信アドレス等、下書き自動生成の対象となる全カテゴリ）を選択する
3. 保存すると、選択したカテゴリの下書き生成処理（各カテゴリの詳細設計のStep3以降に相当する処理）が再実行される

対象範囲：特定のカテゴリに限らず、下書き自動生成の対象となる全カテゴリで共通して使える機能とする（親仕様5章）。特定受信アドレス一覧は宛先ベースの判定（詳細設計_特定受信アドレス.md Step2）のため、本機能の対象に含めるかは13章Open Questions参照。

なお、定型的な問い合わせ対応のカテゴリ判定（AI判定）については、メール1件ずつのカテゴリ変更に加えて、一時的なしきい値・期間を指定した一括再処理の仕組みを別途設ける（詳細設計_下書き自動生成.md 3章Step2「ダッシュボードからの一時的なしきい値変更による再処理」参照。画面配置はシステム設定画面〔11章〕に含める。2026-09-17決定）。

## 7. 特定受信アドレス一覧

詳細設計_特定受信アドレス.md 4章Step5・8章の内容をそのまま踏襲する。

本画面へは、全体サマリー（2章）のカテゴリ別受信件数の円弧グラフのうち「特定受信アドレス」の弧をクリックすることで遷移する（2026-09-17決定）。

表示項目：受信日時、会社名、担当者名、メールアドレス、電話番号、要約、種別（問合せ／営業／他）、フォーム選択カテゴリ、元メールへのリンク、対応状況。

操作：
- 種別によるフィルタ表示（タブ分けはしない）
- 対応状況バッジ（未対応／対応済み／保留）のクリック切り替え
- 種別の手動訂正（誤判定時、決定済み事項）
- 「返信下書きを作成」ボタン（Step 6。作成後は「下書き作成済み」であることが分かる表示を追加）
- 「フォーマット未認識」「処理エラー」フラグの表示（例外処理6章）

## 8. 迷惑メール誤判定検知 結果一覧

迷惑メール誤判定検知機能（親仕様6章）が「正規メールの可能性が高い」と判定したメールを一覧表示する（2026-09-03追加）。

本画面へは、全体サマリー（2章）のカテゴリ別受信件数の円弧グラフのうち「迷惑メール」の弧をクリックすることで遷移する（2026-09-17決定）。

表示項目：
- 送信元メールアドレス
- 送信元企業名および担当者名
- メール件名
- メール分類（メルマガ系／広告系／支払系／有効期限系／その他）

操作：
- 迷惑メールフォルダから受信トレイへ戻す操作のUIは、迷惑メール誤判定検知機能自体の詳細設計とあわせて別途定める（13章Open Questions参照）

## 9. 処理エラーアラート一覧

各機能（下書き自動生成・日程調整アポイント・特定受信アドレス・迷惑メール誤判定検知など）の動作状況を横断的に一覧表示する（2026-09-03追加）。

### 各機能の最終実行日時（稼働状況、2026-09-16追加）

処理エラーアラート一覧の冒頭に、4機能それぞれの「最終実行日時」を表示する小さな一覧を設ける。

- 表示項目：機能名、最終実行日時
- **判定ロジック**：ダッシュボードを開いた（画面を表示した）時点で、現在時刻と各機能の最終実行日時を比較する。各機能の想定スキャン間隔（デフォルト5分。共通基盤_GAS運用の注意点.md参照）を超えて更新がない場合、その機能の「最終実行日時」の文字色を赤く表示する（2026-09-16決定）
- これにより、GASのトリガーが停止して動かなくなる不具合（共通基盤_GAS運用の注意点.md 2(b)参照）を、ダッシュボードを開いた利用者が一目で気づけるようにする
- この比較処理はサーバー側（Spring Boot）で実行する（2026-09-16決定。ダッシュボードの実装方式〔Spring Boot API＋HTML/CSS/JS〕の確定に伴う。共通基盤_GAS運用の注意点.md 8章・詳細設計_ダッシュボード実装方式.md 10章参照）

表示項目（エラーの一覧本体）：
- 動作時間
- 機能名
- 動作結果（正常終了／エラー）
- **エラー種別（通常エラー／要再認証）**（2026-09-15追加）。新着メール検知の連携方式は全カテゴリ（下書き自動生成・日程調整アポイント・特定受信アドレス・迷惑メール誤判定検知）でGAS（Google Apps Script）に統一する方針が決定している（メールアシスタントツール_仕様書.md 4章・7章参照）。GASでGmail連携の認可（OAuth）が失効した場合、放置しても自然には復旧せず人手による再認可操作が必要になる（共通基盤_GAS運用の注意点.md参照）。この種のエラーは、GAS側で「Authorization is required to perform that action」等の特定メッセージを含む例外として検知できるため、各カテゴリの処理側でtry/catchにより通常のエラーと区別し、本項目で「要再認証」として記録する。この検知ロジックは各カテゴリの詳細設計の例外処理章にも個別に明記する
- エラーの場合のエラー内容（「要再認証」の場合は、再認可の手順を示す簡易マニュアル〔スクリーンショット付き〕へのリンクをあわせて表示する。マニュアルの内容は共通基盤_GAS運用の注意点.md 2(d)・4章の手順を元に作成する）

備考：
- 既存の3つの詳細設計（下書き自動生成・日程調整アポイント・特定受信アドレス）は、いずれも自カテゴリの例外処理の章から本一覧への記録を明記済み（2026-09決定・反映済み）。下書き自動生成・日程調整アポイントは処理エラー時「通常の未対応メールとして残す（ダッシュボード上は『未対応』のまま）」に加えて本一覧にも記録し、特定受信アドレスは専用一覧（7章）の「処理エラー」フラグ表示に加えて本一覧にも記録する
- 「要再認証」種別は、GASを採用する全カテゴリ（下書き自動生成・日程調整アポイント・特定受信アドレス・迷惑メール誤判定検知）で共通して用いる（各カテゴリの詳細設計の例外処理章を参照）。表示上は「通常エラー」よりも目立つ形（例：警告色のバッジ、一覧の上位固定表示など）にすることが望ましい
- 本一覧内の個別表示に加えて、画面右上の共通ヘッダーにOAuth連携状態バッジを常設し、認可エラー検知時に赤色表示することで、一覧を開かなくても状態を把握できるようにする（2026-09-15決定。1章「共通ヘッダー：OAuth連携状態バッジ」参照）
- ログの保存期間は親仕様7章「データの取り扱い」参照（暫定：当面は全件保持）

## 10. （廃止）受信メールのグループ化・フォルダ表示

**本機能は削除された（2026-09決定。メールアシスタントツール_仕様書.md 7章「決定済み事項」参照）。** 理由：既に「定型的な問い合わせ対応」「日程調整・アポイント」「特定受信アドレス宛」の3カテゴリ＋「未分類メール」一覧でダッシュボード表示が整理できているため、それ以上の分類機能は不要と判断したため。

以下は削除前の設計内容（記録として残す。実装は行わない）：

~~親仕様5章の内容を踏襲する。特定パターンの送信元からのメールを自動グループ化して表示する。対象パターンはシステム設定画面（11章）で管理する。~~

## 11. システム設定画面

ダッシュボードにバッジを設け、そこからシステム全体の設定画面を呼び出す（2026-09-03追加。2026-09-17決定：呼び出し手段はバッジのみとし、メニュー方式は設けない）。

設定対象項目：
- 稼働曜日・稼働時間帯、想定所要時間、候補日の検索期間（詳細設計_日程調整アポイント.md 3章）
- 日程調整・アポイントのカテゴリ判定用キーワード・観点リスト（同4章Step2）
- 特定受信アドレス一覧、抽出項目のラベル別名一覧（詳細設計_特定受信アドレス.md 3章）
- 迷惑メール誤判定検知機能のスキャン頻度（親仕様6章）
- 定型的な問い合わせ対応のカテゴリ判定用「該当しやすい特徴／該当しにくい特徴」リスト、および実運用で使う恒久的な判定しきい値（初期値0.5。詳細設計_下書き自動生成.md 3章Step2。2026-09追加・決定）

なお、この恒久的なしきい値の変更（本画面での操作）とは別に、一時的なしきい値と対象期間（受信日時の範囲）を指定してメールを再判定する機能を設ける（詳細設計_下書き自動生成.md 3章Step2「ダッシュボードからの一時的なしきい値変更による再処理」参照。2026-09決定）。この一時的な再処理機能は、新たに専用画面を設けず、本設定画面の機能として含める（2026-09-17決定）。

画面構成は、Windows 11の設定アプリのようなイメージとする：左側に設定カテゴリの一覧（縦方向のナビゲーションリスト）を配置し、右側に選択中のカテゴリの詳細設定項目を表示する2ペイン構成とする（2026-09-17決定）。

## 12. 例外処理

- 各カテゴリの処理機能からのデータ連携が一時的に取得できない場合、該当セクションには「データ取得中」または「取得エラー」の表示を行い、他セクションの表示は継続する
- 「不要」操作でGmail側の下書き削除に失敗した場合、ダッシュボード上の一覧からは削除せず、エラーを処理エラーアラート一覧（9章）に記録する

## 13. このカテゴリ固有のOpen Questions

- 処理エラーアラート一覧・迷惑メール誤判定検知結果一覧のログ保存期間（親仕様7章と連動）
- 迷惑メール誤判定検知結果一覧からの「受信トレイへ戻す」操作の具体的な実装方法（UI自体は一覧の行から操作する形で決定済み。GmailApp側でどのAPIを使うかは未定。詳細設計_迷惑メール誤判定検知.md 8章参照）
- 手動カテゴリ再分類機能の対象に特定受信アドレス（宛先ベース判定）を含めるか
- ダッシュボード全体の更新頻度・リアルタイム性、および利用者範囲（親仕様7章の暫定方針を参照。正式確定は運用開始後）
- 各セクション間のナビゲーションUI（左メニュー／タブ／トップページからのリンクなど。カテゴリ別受信件数の円弧グラフクリックによる各一覧への遷移は決定済み〔下記「決定済み事項」参照〕だが、それ以外の画面全体のメニュー構成は未定）
- 処理エラーアラート一覧内の「要再認証」表示の具体的な強調デザイン（バッジの色・一覧内での並び順など。9章参照。画面右上の共通OAuthバッジの見た目は下記「決定済み事項」参照）
- 再認可手順マニュアル（スクリーンショット付き）の保管場所・作成タイミング（9章参照）
- 「個別リトライ」操作の具体的なUI（ボタン単体か、クリックで一覧を開く形か等。設置位置〔OAuthバッジの近く〕は決定済み。1章参照）

### 決定済み事項（参考）
以下は検討の結果、本ドキュメント内で決定済みとした項目。
- **OAuth連携状態バッジの基本デザイン**：画面右上の共通ヘッダーに常設し、通常時は濃いグレーで「OAuth」と表示、いずれかのカテゴリで認可エラーを検知した場合は赤色に変わる（2026-09-15決定。1章「共通ヘッダー：OAuth連携状態バッジ」参照）
- **OAuthバッジのクリック時遷移先**：再認可手順マニュアルへ直接リンクする（処理エラーアラート一覧の該当行へ遷移する方式は採らない）（2026-09-17決定。1章・9章参照）
- **個別リトライ操作の設置位置**：共通ヘッダーのOAuthバッジのすぐ近くに設置する（2026-09-16決定。1章参照）
- **各機能の最終実行日時の異常表示ルール**：ダッシュボードを開いた時点で、想定スキャン間隔（デフォルト5分）を超えて更新がなければ、最終実行日時の文字色を赤くする（2026-09-16決定。9章参照）
- **GASの時間主導型トリガーのスキャン間隔（ポーリング頻度）のデフォルト値**：4機能共通で5分とする（2026-09-16決定。共通基盤_GAS運用の注意点.md参照）
- **各機能の「最終実行日時」の赤字判定処理をどこで実行するか**：ダッシュボードの実装方式（Spring Boot API＋HTML/CSS/JS）が確定したため、サーバー側（Spring Boot）で実行する方針とした（2026-09-16決定。9章・詳細設計_ダッシュボード実装方式.md参照）
- **下書き修正率のゲージ表示**：実装しない（2026-09決定。メールアシスタントツール_仕様書.md 7章の決定に合わせ、2章から削除済み）
- **受信メールのグループ化・フォルダ表示機能**：削除する（2026-09決定。メールアシスタントツール_仕様書.md 7章の決定に合わせ、10章を廃止済み）
- **「不要」操作時の下書き削除範囲**：Gmail側の下書き自体も削除する（ダッシュボードの管理データのみを削除して表示から外す方式は採らない）（2026-09-17決定。3章参照）
- **カテゴリ別受信件数の円弧グラフのクリック操作**：クリックすると対応する一覧画面（要確認の下書き一覧〔3章〕、特定受信アドレス一覧〔7章〕、迷惑メール誤判定検知結果一覧〔8章〕等）に遷移する（2026-09-17決定。2章参照）
- **カテゴリ別受信件数の内訳**：社外／社内の区分は廃止し、迷惑メール誤判定検知を区分の1つとして含める（2026-09-17決定。2章参照）
- **システム設定画面へのアクセス手段**：メニューは設けず、バッジのみから呼び出す（2026-09-17決定。11章参照）
- **一時的なしきい値再処理機能の設置画面**：新規の専用画面は設けず、システム設定画面（11章）の機能として含める（2026-09-17決定）
- **システム設定画面の画面構成**：Windows 11の設定アプリのような2ペイン構成（左：設定カテゴリの一覧、右：選択中カテゴリの詳細設定項目）とする（2026-09-17決定。11章参照）

## 14. 他機能との関係

- 下書き自動生成／日程調整アポイント／特定受信アドレス：各機能の出力（下書きステータス、保留理由、注意メッセージ、集計用メタ情報）がダッシュボード表示の一次データソースとなる
- 迷惑メール誤判定検知機能（親仕様6章）：結果一覧（8章）の一次データソース

## 15. 実装方式

ダッシュボード自体の技術スタック・API仕様は`詳細設計_ダッシュボード実装方式.md`にまとめた（2026-09-16決定。フロントエンド：HTML/CSS/JS、バックエンド：Java（Spring Boot）のREST API、データストア：PostgreSQL。GASの4機能とはHTTP経由で連携する）。
