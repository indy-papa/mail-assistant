# 詳細仕様：日程調整・アポイント機能

対象カテゴリ：日程調整・アポイント
親仕様：メールアシスタントツール_仕様書.md（v0.21）2章・3章・4章

作成日: 2026-08-28

## 1. トリガー

GAS（Google Apps Script）の時間主導型トリガーにより、Gmailの新着メールを定期的にポーリングして検知し、処理を開始する（2026-09-15決定。全カテゴリ共通の方式。親仕様4章参照）。ポーリング間隔（スキャン頻度）のデフォルト値は5分とする（2026-09-16決定。下書き自動生成・日程調整アポイント・特定受信アドレス・迷惑メール誤判定検知の4機能共通のデフォルト値。共通基盤_GAS運用の注意点.md参照）。

## 2. 前提・追加の連携要件

本機能の実現には、Gmailコネクタに加えてGoogleカレンダーコネクタとの連携が別途必要（親仕様2章参照）。カレンダーの「読み取り」（空き時間確認）と「書き込み」（予定の自動作成）の両方の権限が必要になる。

## 3. 入力

- メール本文（プレーンテキスト）
- 件名
- 差出人アドレス
- 受信日時
- スレッド履歴（同一スレッド内の過去のやり取り。あれば）
- 除外判定用のラベル情報（迷惑メール判定結果）
- Googleカレンダーの予定情報（対象期間内の予定一覧）
- 稼働曜日・稼働時間帯の設定値（利用者が設定画面で指定・変更できる。**時間帯は固定の1つの範囲とし、曜日ごとに異なる時間帯を設定する機能は設けない。稼働日として扱う曜日は個別に選択できる**ようにする。未設定時のデフォルトは「稼働曜日：月〜金、稼働時間帯：9:00〜18:00」とする）
- 打ち合わせの想定所要時間（**利用者が設定画面でいつでも値を変更できる。ただし相手・案件ごとに異なる値を使い分ける機能は設けず、常に同じ1つの値をすべてのメールに一律適用する**。未設定時のデフォルトは60分とする。フェーズ1ではメール文面からの所要時間の抽出は行わない）
- 候補日の検索期間の設定値（**利用者が設定画面で指定・変更できる**。未設定時のデフォルトは2週間とする。4章Step 4-A・Step 4-A補足参照）
- カテゴリ判定用のキーワード・観点リスト（利用者が設定画面で追加・編集する設定値。4章Step 2参照）

## 4. 処理フロー

### Step 1：除外判定（前処理）

以下に該当するメールは、本機能の処理対象から外す。

- 迷惑メールフォルダに分類されているメール（親仕様6章）

該当する場合はここで処理を打ち切り、迷惑メール誤判定検知機能側の警告・通知フローに委ねる（下書き自動生成の詳細設計と共通のルール）。

### Step 2：カテゴリ判定

メール内容が「日程調整・アポイント」に該当するかを判定する。

判定の観点（案）：
- 日程・都合に関する表現（「ご都合のよい日程」「打ち合わせ」「面談」「〇日はいかがでしょうか」等）が含まれるか
- 具体的な日時候補（曜日・日付・時刻）の記載があるか
- 「予定」「アポ」「MTG」等のキーワードが含まれるか

該当しないと判定された場合は、他カテゴリ（定型的な問い合わせ対応／営業・取引先／特定受信アドレス）の判定ロジックに回す。どのカテゴリにも該当しない場合は、下書き生成をスキップし、通常の未分類の受信メールとして扱う（下書き自動生成の詳細設計と共通の切り分け方針）。

#### 判定基準（キーワード・観点）の運用

- 上記の判定観点・キーワードは、コードに固定で埋め込むのではなく、利用者が設定画面から追記・編集できる**一覧（設定リスト）として管理**する
- 判定漏れ（本来は日程調整・アポイントに該当するのに、他カテゴリ扱いや未分類のままスルーされたメール）に気づいた場合、利用者はその原因となった観点・キーワードを一覧に随時追記できる
- 追記した基準は**以降に受信するメールから適用**する。過去に見逃した（他カテゴリ扱い・未分類扱いになった）メールを新基準で遡及的に再スキャンする仕組みは**用意しない**（フェーズ1では実装のシンプルさを優先し、過去分は対象外とする）
- 過去に見逃したメール自体への対応は、次の「手動再分類」で個別に拾う
- なお、このキーワード一覧の設定画面をどう設計するか（本カテゴリ専用にするか、他カテゴリと共通の管理画面にまとめるか）は、8章のOpen Questions参照

#### 判定漏れメールの手動再分類（ダッシュボード連携）

- 利用者が判定漏れのメール（他カテゴリ扱い・未分類のまま残っているメール）に気づいた場合、**ダッシュボードから該当メールのカテゴリを手動で「日程調整・アポイント」に付け直し、その場で下書き生成処理（Step 3以降）を再実行できるようにする**
- この手動再分類機能は日程調整・アポイントに限らず、他カテゴリ（定型的な問い合わせ対応など）でも共通して使えるダッシュボード側の機能として位置づける（親仕様5章・`機能一覧.md`のダッシュボード機能に追加。本機能固有の要件ではないため、詳細な画面仕様はダッシュボード側の設計で別途まとめる）

### Step 3：サブケース分岐

メール内容から、以下の2パターンのいずれに該当するかを判定する。

**パターンA：こちらが候補日を提示する必要があるケース**
- 相手から「ご都合の良い日程を教えてください」等、日程調整を持ちかけられた場合
- 自分から日程調整を持ちかける返信を作る場合（例：資料送付後に「詳細は打ち合わせで」という流れになった場合など）

**パターンB：相手が候補日を提示しているケース**
- 相手のメールに具体的な日時候補（1件以上）が記載されている場合

いずれにも明確に該当しない場合（日程調整カテゴリとは判定されたが、パターンの判別が曖昧な場合）は保留とし、ダッシュボードに「要確認（日程調整・判定不明）」として表示する。

### Step 4-A：候補日の抽出・提示（パターンA）

1. Googleカレンダーで、設定済みの稼働曜日・時間帯の範囲内、**設定済みの検索期間（未設定時のデフォルトは2週間）**の予定を取得する
2. 設定済みの想定所要時間（未設定時はデフォルト60分）の空き枠を検索する
3. 空き枠から**3件**を選定する（選定ルール案：直近のものから順に3件。当日・翌日など準備期間が短すぎる枠を除外すべきかは8章のOpen Questions参照）
4. 選定した候補日を記載した返信下書きを作成する
5. Step 5（下書き保存・通知）へ

空き枠が3件に満たない場合の扱いは以下の通り。

- **1〜2件見つかった場合**：見つかった件数分だけ提示する（検索期間の延長は行わない）
- **0件だった場合**：検索期間を**1回だけ自動延長**して再検索する。延長幅は検索期間の設定値と同じ幅を追加する（例：デフォルト設定＝2週間の場合、2週間＋2週間で計4週間分を検索する。延長幅専用の設定項目は設けず、検索期間の設定値をそのまま延長幅としても使う）。延長後の再検索でも0件の場合は、そこで打ち切り、保留として扱い、ダッシュボードに「要確認（日程調整・候補日なし）」として表示する

### Step 4-B：相手提示日の確認・予定登録（パターンB）

1. メール本文から日時候補を抽出する（複数ある場合はすべて抽出する）
2. 抽出した各候補についてGoogleカレンダーの空き状況を確認する（判定に使う所要時間は3章の設定値）
   - 日時表現が曖昧（「来週後半」等、具体的な日付・時刻に落とし込めないもの）な場合、その候補は「判定不可」として扱い、空き確認の対象から除外する
   - **この場合、他に有効な候補があり処理自体は続行できるときも、ダッシュボードに「要確認（日程調整・一部候補の日時解析不可）」という注意メッセージを表示し、判定不可とした候補が存在したことを利用者に知らせる**（自動処理が候補を取りこぼしている可能性を見逃さないため）
3. **空いている候補が1件以上ある場合：**
   - 相手が第一希望を明示していればそれを優先する。優先順位の言及が特になければ、抽出順（＝メール記載順）で最初に空いている候補を採用する
   - 採用した日時でGoogleカレンダーに予定を**自動作成**すると同時に、相手への確定連絡の返信下書きを自動作成する（この2つは常にセットで実行する。カレンダー登録は下書きではなく即時実行、返信メールは下書きとして人の確認を待つ。親仕様3章に明記された挙動）
     - イベントタイトルの**先頭に「(MA)」を付与**する（例：「(MA) 〇〇様 打ち合わせ」）。MAは「メールアシスタント」の略で、本ツールが自動でスケジュール登録したものであることを示す。**このプレフィックスは、内容確認後も自動では削除しない**（利用者が必要に応じて手動で編集する運用とする）
     - **相手はGoogleカレンダーの参加者として招待しない（自分のみの予定として登録する）**。参加者への招待が必要な場合は、利用者が内容確認後に手動で追加する運用とする
   - 返信下書きには、採用した日時を明記する
   - ダッシュボードに「自動で予定を登録しました」という注意メッセージを表示する（親仕様3章に明記された挙動。「要確認の下書き」の告知とは別枠の注意表示とする）
4. **提示された候補がすべて埋まっている場合：**
   - 予定は作成しない
   - 代替の候補日を提示する返信下書きを作成する（Step 4-Aのロジックを流用し、新たに3件を提示する）
   - **ダッシュボードに「要確認（日程調整・提示候補すべて埋まっていたため代替日を提示）」という注意メッセージを表示する**（相手の希望日には対応できなかったことを利用者が把握できるようにするため）
5. Step 5（下書き保存・通知）へ

### Step 5：下書き保存・通知

- Gmailに、元メールへの返信として下書きを保存する
- パターンBで予定を自動登録した場合は、ダッシュボードのステータスを他カテゴリと区別できる状態（例：「予定登録済み・下書き確認待ち」）にする
- ダッシュボードのステータスを「下書き作成済み・未送信」に更新する（親仕様5章「要確認の下書き」の告知）

## 5. 出力

- Gmail下書き（元メールへの返信形式）
- （パターンBかつ空きありの場合のみ）Googleカレンダーへの予定登録（タイトル先頭に「(MA)」を付与、自分のみの予定として登録・相手は招待しない）
- ダッシュボードのステータス更新
- ダッシュボードへの注意メッセージ表示（パターンBでの自動予定登録時、一部候補の日時解析不可時、提示候補がすべて埋まっていた時、検索期間を延長しても候補日が0件だった時のいずれか該当する場合）
- 集計用のメタ情報（カテゴリ、パターンA/B、自動登録の有無など。親仕様5章の全体サマリーグラフの集計対象）

## 6. 例外処理

- **Googleカレンダー連携がエラーになった場合**：処理を中断し、下書きは作らず、通常の未対応メールとして残す。あわせて処理エラーアラート一覧（詳細設計_ダッシュボード.md 9章）にも記録する（2026-09決定）
- **Gmail連携またはGoogleカレンダー連携の認可（OAuth）が失効している場合**：GASでは「Authorization is required to perform that action」等の特定メッセージを含む例外が発生するため、try/catchで検知し、上記の一般的な連携エラーとは区別して処理エラーアラート一覧に「要再認証」種別で記録する（2026-09-15決定）。この種のエラーは放置しても自然には復旧せず、人手による再認可操作が必要となるため区別する。本カテゴリはGmailとGoogleカレンダーの2つの連携を使うため、どちらの認可が失効した場合も同様に扱う。全カテゴリ共通の方針（詳細は`共通基盤_GAS運用の注意点.md`、`メールアシスタントツール_仕様書.md` 7章「Gmail連携の認可エラー対応方針」参照）
- **同一スレッド内に既に確定済みの予定（自動登録済み、または手動確認済み）がある場合**：重複して新たな予定は作らない。日程再調整（変更依頼）の申し出はフェーズ1のスコープ外のため、検知した場合は通常の未分類メールとして扱う
- **同一スレッド内に既に未送信の下書きが存在する場合**：重複して新しい下書きを作らない（下書き自動生成の詳細設計と共通のルール）
- **メール中のすべての日時候補の解析に失敗した場合**（有効な候補が1件も残らない場合）：保留とし、ダッシュボードに「要確認（日程調整・日時解析不可）」として表示する。一部の候補のみ解析不可の場合はStep 4-B参照
- **検索期間を1回延長しても候補日が0件のままの場合**：保留とし、ダッシュボードに「要確認（日程調整・候補日なし）」として表示する（Step 4-A参照）
- **稼働曜日・時間帯、想定所要時間、または検索期間が未設定の場合**：デフォルト値（稼働曜日：月〜金、稼働時間帯：9:00〜18:00、想定所要時間：60分、検索期間：2週間）を使用する

## 7. 他機能との関係

- ダッシュボード（親仕様5章）：「要確認の下書き」の告知に加え、パターンBでの自動予定登録時・一部候補解析不可時・候補全滅時・候補日0件時の各種注意メッセージ表示、全体サマリーでの日程調整カテゴリの件数集計、判定漏れメールの手動再分類機能（4章Step 2参照）との連携
- モードB（全自動送信、親仕様3章）：パターンA（候補日提示）は比較的定型的な作業のため、将来モードBへの昇格候補として優先度が高いと考えられる。一方パターンB（予定の確定登録を伴う）は実際にカレンダーを動かす操作であるため、より慎重な検証が必要（親仕様7章Open Questions参照）
- 下書き自動生成（定型的な問い合わせ対応）：Step 2のカテゴリ判定で日程調整に該当しないと判定されたメールは、そちらのロジックに回される

## 8. このカテゴリ固有のOpen Questions

- 候補日3件の選定ルールの精緻化（直近順で機械的に決めてよいか、当日・翌日など準備期間が短すぎる枠を除外すべきか）
- 相手の希望条件（オンライン／対面、場所、Google Meetリンクの要否など）をどこまで下書き・予定登録に反映するか（**フェーズ1では反映しない。運用を開始してから、実際にどの程度必要になるか・仕様変更する妥当性があるかを見極めたうえで、対応するかどうかを改めて判断する**）
- 判定観点・キーワード一覧の設定画面の項目設計（このカテゴリ専用の一覧にするか、他カテゴリと共通の判定基準管理画面にまとめるか）→ **全体的な設計が終わった段階で改めてまとめる（現時点では先送り）**
- 日程変更・キャンセル依頼、3人以上が絡む多人数調整への対応を将来のフェーズでどう追加するか（フェーズ1では対象外とすることを確認済み。親仕様7章とも関連）

### 決定済み事項（参考）
以下は検討の結果、本ドキュメント内で決定済みとした項目（旧Open Questions）。詳細は各Stepの本文を参照。
- 予定自動登録時、相手をGoogleカレンダーの参加者として招待するかどうか → **招待しない。自分のみの予定として登録し、招待が必要な場合は利用者が手動で設定する**（Step 4-B）
- イベントタイトルの「(MA)」プレフィックスの扱い → **確定後も自動では削除せず、残しっぱなしにする**（Step 4-B）
- 稼働曜日・時間帯の設定粒度 → **時間帯は固定1つ、稼働曜日は個別に選択可能。値自体は設定画面でいつでも変更できる**（3章）
- 想定所要時間の設定粒度 → **値は設定画面でいつでも変更できる。ただし相手・案件ごとに複数の値を使い分ける機能は設けず、常に1つの値を共通適用する**（3章）
- 候補日の検索期間（デフォルト2週間）→ **設定画面で変更可能にする**（3章）。空き枠が0件だった場合は**検索期間を1回だけ自動延長する（延長幅は検索期間と同じ値を使う。専用の延長幅設定は設けない）。延長後も0件なら保留にする**（Step 4-A）
